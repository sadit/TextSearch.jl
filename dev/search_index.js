var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#TextSearch-API","page":"API","text":"","title":"TextSearch API"},{"category":"method","location":"api/#Base.sum-Union{Tuple{AbstractVector{<:SparseArrays.SparseVector{Tv, Ti}}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API","text":"Base.sum(cluster::AbstractVector{<:SparseVector})\n\nSparseVector counterpart of sum(::AbstractVector{<:Dict}): concatenate every (index, value) pair from every input vector, sort once by index, then combine consecutive equal indices in a single linear pass. Beat every alternative tried (naive +-folding, pairwise tree merging, a k-way heap merge) at every cluster size benchmarked, and — unlike a dense-accumulator approach — its cost does not depend on the vectors' dimension, only on their total number of stored entries. See sadit/TextSearch.jl#25.\n\nAll vectors in cluster must have the same dimension (length).\n\n\n\n\n\n","title":"Base.sum"},{"category":"method","location":"api/#Distances.evaluate-Tuple{SimilaritySearch.Dist.NormCosine, SparseArrays.SparseVector, SparseArrays.SparseVector}","page":"API","text":"evaluate(::NormCosine, a::SparseVector, b::SparseVector)::Float64\nevaluate(::Cosine, a::SparseVector, b::SparseVector)::Float64\nevaluate(::NormAngle, a::SparseVector, b::SparseVector)::Float64\nevaluate(::Angle, a::SparseVector, b::SparseVector)::Float64\n\nSparseVector counterparts of the Dict-based distance functions above, using sparsedot instead of the plain-merge dot for the underlying inner product. NormCosine/NormAngle assume a/b are already normalized; Cosine/Angle do not.\n\nExample\n\njulia> using SparseArrays\n\njulia> evaluate(NormCosine(), sparsevec(UInt32[1, 2], Float32[0.6, 0.8], 10), sparsevec(UInt32[2], Float32[1.0], 10))\n0.19999998807907104\n\n\n\n\n\n","title":"Distances.evaluate"},{"category":"method","location":"api/#SparseArrays.sparsevec-Union{Tuple{Dict{Ti, Tv}}, Tuple{Tv}, Tuple{Ti}, Tuple{Dict{Ti, Tv}, Integer}} where {Ti<:Integer, Tv<:Number}","page":"API","text":"sparsevec(vec::Dict{Ti,Tv}, m=0) where {Ti<:Integer,Tv<:Number}\n\nCreates a sparse vector from a Dict-based sparse vector\n\nExample\n\njulia> sparsevec(Dict{UInt32,Float32}(1 => 0.5, 3 => 0.2))\n  [1]  =  0.5\n  [3]  =  0.2\n\n\n\n\n\n","title":"SparseArrays.sparsevec"},{"category":"function","location":"api/#TextSearch.BinaryInvertedFile","page":"API","text":"BinaryInvertedFile(vocsize::Integer, dist=Dist.Sets.Jaccard())\n\nCreates an empty InvertedFile indexed by a set distance (e.g. Dist.Sets.Jaccard(), Dist.Sets.Dice(), Dist.Sets.Intersection(), Dist.Sets.CosineSet()), suitable for set/token-membership objects (sets or sorted vectors of integer ids).\n\n\n\n\n\n","title":"TextSearch.BinaryInvertedFile"},{"category":"method","location":"api/#TextSearch.WeightedInvertedFile-Tuple{Integer}","page":"API","text":"WeightedInvertedFile(vocsize::Integer)\n\nCreates an empty InvertedFile indexed by cosine dissimilarity (Dist.NormCosine()), suitable for weighted sparse vectors (e.g. SparseVectors produced by vectorize).\n\n\n\n\n\n","title":"TextSearch.WeightedInvertedFile"},{"category":"method","location":"api/#TextSearch._bow_sizehint-Tuple{Vocabulary}","page":"API","text":"_bow_sizehint(voc::Vocabulary)\n\nEstimated final size (number of unique tokens) of a BOW computed under voc, used to sizehint! it up front and avoid rehashing while it's filled. Uses voc's own avgdoclen (average tokens per document across its training corpus) as the estimate, falling back to a small default before voc has seen any training documents.\n\n\n\n\n\n","title":"TextSearch._bow_sizehint"},{"category":"method","location":"api/#TextSearch._common_prefix_len-Tuple{Vector{Char}, Vector{Char}}","page":"API","text":"_common_prefix_len(a, b) -> Int\n\nNumber of leading characters a and b share (both given as Char vectors).\n\n\n\n\n\n","title":"TextSearch._common_prefix_len"},{"category":"method","location":"api/#TextSearch._decode_snowball_transformation-Tuple{AbstractString, AbstractString}","page":"API","text":"_decode_snowball_transformation(algorithm::AbstractString, charenc::AbstractString)\n\nReconstructs a SnowballTokenTransformation from a profile by dispatching into TextSearchSnowballExt (the Snowball/Languages package extension), looked up at runtime via Base.get_extension rather than as an overridable method – a package extension is not allowed to overwrite a method already defined in the parent package, so the extension instead defines a same-named function inside its OWN module (TextSearchSnowballExt._construct_snowball_transformation), which this looks up dynamically. Errors clearly if the extension isn't loaded yet, naming what to using first.\n\n\n\n\n\n","title":"TextSearch._decode_snowball_transformation"},{"category":"method","location":"api/#TextSearch._decode_transformation-Tuple{Any, Function}","page":"API","text":"_decode_transformation(d, read_file::Function)\n\nDecodes a transformation's tagged-union JSON d back into an AbstractTokenTransformation; read_file(name::AbstractString) fetches and JSON3-parses a referenced file (\"stopwords\" kind) – the caller supplies one that reads from a plain directory or from an open zip archive, so this function itself doesn't care which.\n\n\n\n\n\n","title":"TextSearch._decode_transformation"},{"category":"function","location":"api/#TextSearch._encode_transformation_with_files","page":"API","text":"_encode_transformation_with_files(tt, counter=Ref(0)) -> (json, files)\n\nEncodes tt into its tagged-union JSON representation, EXCEPT any IgnoreStopwords step's word list, which is instead written to its own \"stopwords.json\"-style file (numbered on a second, third, ... occurrence via counter): json gets a \"file\" reference instead of an inlined \"words\" array, and files collects filename => word_vector pairs to be written by the caller (save_profile).\n\n\n\n\n\n","title":"TextSearch._encode_transformation_with_files"},{"category":"method","location":"api/#TextSearch._fuse_synonyms-Tuple{Any, Vocabulary, Integer, Real}","page":"API","text":"_fuse_synonyms(profiles, voc, k, rrf_k) -> Dict{String,Vector{Pair{String,Float32}}}\n\nFuses the per-profile synonym networks into one.\n\nEach input profile fit its own encoder, so its neighbor distances live in its own embedding space and are not numerically comparable across profiles – averaging them directly would be meaningless. What does transfer is the ranking: a token that several independently-fit profiles all place near the same neighbor is far likelier to be a real relation than one that a single profile ranked highly. So the lists are combined with Reciprocal Rank Fusion, score(candidate) = Σ_p 1/(rrf_k + rank_p), the standard way to merge ranked lists produced by incomparable scorers.\n\nThe Float32 kept alongside each surviving neighbor is the mean of the distances the contributing profiles reported for that pair – informative (cosine distances share a scale even across spaces) but, unlike a single profile's synonyms, no longer a distance in any one space. Candidates are restricted to tokens that survive in the merged vocabulary.\n\n\n\n\n\n","title":"TextSearch._fuse_synonyms"},{"category":"method","location":"api/#TextSearch._leader_groups-Union{Tuple{T}, Tuple{Vector{T}, Vector{Int64}, Any}} where T","page":"API","text":"_leader_groups(items, order, close) -> Vector{Vector{T}}\n\nGroups items around seeds: walking them in order, the first unassigned item becomes a seed and every still-unassigned item that is close to that seed joins it.\n\nThis deliberately replaces single-linkage for morphology. Single linkage chains – A~B and B~C merge even when A and C are unrelated – and on a real vocabulary the chains swallow everything sharing a prefix: measured on 143k Spanish tokens it produced a 292-member \"family\" spanning concentra...cons, and merged cara with caracas and caracalla. Requiring closeness to the seed instead bounds every group by one radius around its lemma, which is also exactly the shape \"a lemma plus its variants\" should have.\n\nVisiting in the selector's own order (see _selector_key) makes the seed the token the selector would have elected anyway.\n\n\n\n\n\n","title":"TextSearch._leader_groups"},{"category":"method","location":"api/#TextSearch._link_subclusters-Union{Tuple{T}, Tuple{Vector{T}, Any}} where T","page":"API","text":"_link_subclusters(items, close) -> Vector{Vector{T}}\n\nSingle-linkage grouping of items under the predicate close(i, j) (indices into items), by union-find. O(length(items)^2) predicate calls, so callers must keep the input small (by blocking, or by having partitioned already).\n\n\n\n\n\n","title":"TextSearch._link_subclusters"},{"category":"method","location":"api/#TextSearch._merge_transformations-Tuple{Any}","page":"API","text":"_merge_transformations(tts) -> AbstractTokenTransformation\n\nResolves the token transformation of a merged profile. Identical transformations are kept as-is. Transformations that differ only in their stopword set – the normal case when each batch detected its own stopwords from its own slice – collapse to the union of those sets, since a token any batch removed is absent from that batch's vocabulary and so is already excluded from the merged counts. Anything else (differing stemmers, differing chains) cannot be reconciled and is an error.\n\n\n\n\n\n","title":"TextSearch._merge_transformations"},{"category":"method","location":"api/#TextSearch._morphology_metric-Tuple{Symbol, Int64}","page":"API","text":"_morphology_metric(morphology, qgram) -> (prepare, distance)\n\nBuilds the pair of functions the subclustering needs: prepare(token_string) computes whatever representation the metric compares, and distance(a, b) scores two prepared representations on [0, 1] (0 = identical surface form).\n\n:jaccard: Jaccard distance over character qgram-gram sets, via Dist.Sets.Jaccard. Insensitive to where the difference falls, so it handles prefixal, suffixal and infixal variation alike.\n:levenshtein: edit distance via Dist.Seqs.Levenshtein, normalized by the longer token so a single threshold means the same thing for short and long words.\n\nBoth are normalized deliberately: an absolute edit distance of 2 is negligible between long words and total between short ones, so a raw threshold would behave inconsistently across a real vocabulary.\n\n\n\n\n\n","title":"TextSearch._morphology_metric"},{"category":"method","location":"api/#TextSearch._prefix_blocks-Tuple{Vocabulary, Any, Int64}","page":"API","text":"_prefix_blocks(voc, ids, prefix_len) -> Vector{Vector{UInt32}}\n\nBuckets ids by their tokens' first prefix_len characters. When linking requires a shared prefix of that length, this blocking is exact – two tokens in different buckets can never link – and it is what makes morphology-first clustering affordable: comparing the whole vocabulary pairwise is O(vocsize^2) (10^10 pairs at 143k tokens), while the sum over buckets is smaller by orders of magnitude.\n\nprefix_len <= 0 cannot block, so everything lands in a single bucket – which also means min_common_prefix = 0 gives up the blocking speedup entirely.\n\nRequiring a shared prefix is not only an optimization: character n-gram similarity is position-blind, so without it abioticos/bioticos and abandonadas/donadas link on sharing nearly every gram despite being different words. It encodes that the target language inflects by suffix, so set it to 0 for languages where that does not hold.\n\n\n\n\n\n","title":"TextSearch._prefix_blocks"},{"category":"method","location":"api/#TextSearch._profile_reader-Tuple{AbstractString}","page":"API","text":"_profile_reader(path::AbstractString) -> read_file::Function\n\nReturns a read_file(name::AbstractString) -> JSON3 closure that fetches and parses a named member of the profile at path – a plain directory if isdir(path), otherwise a .zip archive (opened once and re-read from memory for every subsequent read_file call). This is what lets load_profile not care which of the two forms it was handed.\n\n\n\n\n\n","title":"TextSearch._profile_reader"},{"category":"method","location":"api/#TextSearch._qgram_ids-Tuple{AbstractString, Int64, Dict{String, Int32}}","page":"API","text":"_qgram_ids(s, q, vocab) -> Vector{Int32}\n\nSorted, deduplicated ids of s's character q-grams, interning grams through vocab so distinct grams never collide. This is the representation SimilaritySearch.Dist.Sets.Jaccard expects (a sorted set as a vector). Tokens shorter than q are represented by themselves, so they can only match identical tokens.\n\n\n\n\n\n","title":"TextSearch._qgram_ids"},{"category":"method","location":"api/#TextSearch._selector_key-Tuple{Symbol}","page":"API","text":"_selector_key(selector) -> (voc, tid) -> key\n\nSort key matching _lemma_pick's preference, so a leader pass can visit candidates in the order the selector would elect them and its seed is already the lemma.\n\n\n\n\n\n","title":"TextSearch._selector_key"},{"category":"method","location":"api/#TextSearch._semantic_clustering-Tuple{Symbol, Any, Any, Integer, Integer}","page":"API","text":"_semantic_clustering(algorithm, dist, wordvecs, num_clusters, m)\n\nRuns the requested SimilaritySearch clustering over the token embeddings, defaulting num_clusters to ceil(sqrt(m)).\n\n\n\n\n\n","title":"TextSearch._semantic_clustering"},{"category":"method","location":"api/#TextSearch._vote_lemmas-Tuple{Any, Vocabulary}","page":"API","text":"_vote_lemmas(profiles, voc) -> Dict{String,String}\n\nMerges the per-profile token => lemma maps by plurality vote (ties broken by the canonical-token rule: most frequent, then shortest, then lexicographic), keeping only tokens and lemmas that survive in the merged vocabulary.\n\nIndependent votes can disagree in ways a single clustering never does – a => b in some profiles and b => a in others – so the winning edges are then followed to a fixed point so that a whole chain collapses onto one canonical token, and any cycle is resolved by electing its most frequent member. Without that pass the merged map could contain cycles, which would make naive lemma lookup non-terminating.\n\n\n\n\n\n","title":"TextSearch._vote_lemmas"},{"category":"method","location":"api/#TextSearch.avgdoclen-Tuple{Vocabulary}","page":"API","text":"avgdoclen(voc::Vocabulary)\n\nAverage document length in tokens (numtokens(voc) / trainsize(voc)), used by BM25Scorer.\n\n\n\n\n\n","title":"TextSearch.avgdoclen"},{"category":"method","location":"api/#TextSearch.bagofwords!-Tuple{Dict{UInt32, Int32}, Vocabulary, Any}","page":"API","text":"bagofwords!(bow::BOW, voc::Vocabulary, messages)\n\nComputes a bag of words from a multi-field document (a list of texts), accumulating the result into bow. See bagofwords for the non-mutating version.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> bow = BOW();\n\njulia> TextSearch.bagofwords!(bow, voc, \"hello hello world\");\n\njulia> bow\nDict{UInt32, Int32}(0x00000002 => 1, 0x00000001 => 2)\n\n\n\n\n\n","title":"TextSearch.bagofwords!"},{"category":"method","location":"api/#TextSearch.bagofwords!-Tuple{Dict{UInt32, Int32}, Vocabulary, TokenizedText}","page":"API","text":"bagofwords!(bow::BOW, voc::Vocabulary, tokenlist::TokenizedText)\n\nAccumulates the tokens in tokenlist into bow (a BOW), looking up each token's id in voc; out-of-vocabulary tokens are skipped. Returns bow.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> TextSearch.bagofwords!(BOW(), voc, tokenize(TextConfig(), \"hello hello\"))\nDict{UInt32, Int32}(0x00000001 => 2)\n\n\n\n\n\n","title":"TextSearch.bagofwords!"},{"category":"method","location":"api/#TextSearch.bagofwords-Tuple{Vocabulary, Any}","page":"API","text":"bagofwords(voc::Vocabulary, messages; isnormalized::Bool=false)\n\nTokenizes messages (a string or a list of strings) under voc's TextConfig and returns its bag of words (BOW): a token id => occurrence count mapping. An already-computed BOW is returned unchanged.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> bagofwords(voc, \"hello hello world\")\nDict{UInt32, Int32}(0x00000002 => 1, 0x00000001 => 2)\n\n\n\n\n\n","title":"TextSearch.bagofwords"},{"category":"method","location":"api/#TextSearch.bagofwords_corpus-Tuple{Vocabulary, AbstractVector{Dict{UInt32, Int32}}}","page":"API","text":"bagofwords_corpus(voc::Vocabulary, corpus::AbstractVector; isnormalized::Bool=false, verbose=true)\n\nComputes a list of bag of words (BOWs) from a corpus, one per document, in parallel across threads.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> bagofwords_corpus(voc, [\"hello world\", \"hello there\"]; verbose=false)[1]\nDict{UInt32, Int32}(0x00000002 => 1, 0x00000001 => 1)\n\n\n\n\n\n","title":"TextSearch.bagofwords_corpus"},{"category":"method","location":"api/#TextSearch.centroid-Tuple{AbstractVector{<:SparseArrays.SparseVector}}","page":"API","text":"centroid(cluster::AbstractVector{<:SparseVector})\n\nCentroid (normalized sum) of a cluster of SparseVectors. See sum(::AbstractVector{<:SparseVector}) for the algorithm.\n\n\n\n\n\n","title":"TextSearch.centroid"},{"category":"method","location":"api/#TextSearch.decode-Tuple{Vocabulary, Dict}","page":"API","text":"decode(voc::Vocabulary, bow::Dict)\n\nConverts a Dict sparse vector indexed by token id (e.g., a BOW) into a Dict indexed by the corresponding token string.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> decode(voc, bagofwords(voc, \"hello hello\"))\nDict{String, Int32}(\"hello\" => 2)\n\n\n\n\n\n","title":"TextSearch.decode"},{"category":"method","location":"api/#TextSearch.dvec-Tuple{SparseArrays.AbstractSparseVector}","page":"API","text":"dvec(x::AbstractSparseVector)\n\nConverts an sparse vector into a dict-based sparse vector\n\nExample\n\njulia> dvec(sparsevec(Dict{UInt32,Float32}(1 => 0.5, 3 => 0.2)))\nDict{UInt32, Float32}(0x00000003 => 0.2, 0x00000001 => 0.5)\n\n\n\n\n\n","title":"TextSearch.dvec"},{"category":"method","location":"api/#TextSearch.encode-Tuple{Vocabulary, Dict}","page":"API","text":"encode(voc::Vocabulary, bow::Dict)\n\nConverts a Dict sparse vector indexed by token string into a Dict indexed by token id, the inverse of decode.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> encode(voc, Dict(\"hello\" => 2))\nDict{UInt32, Int64}(0x00000001 => 2)\n\n\n\n\n\n","title":"TextSearch.encode"},{"category":"method","location":"api/#TextSearch.expand_synonyms!-Tuple{SparseArrays.SparseVector, Vocabulary, Any}","page":"API","text":"expand_synonyms!(vec::SparseVector, voc::Vocabulary, synonyms;\n                  weight_fn=d -> exp(-d), normalize::Bool=true) -> vec\n\nExpands a query's sparse tf-idf vector IN PLACE with weighted contributions from each present token's synonyms (synonyms, e.g. as produced by LSI.synonyms). This mutates vec – pass an unnormalized, disposable query vector (vectorize(model, query; normalize=false)); never a vector you still need afterwards, and never a document vector (documents are never expanded, only queries). Normalizing before calling this would also make the original-vs-synonym weight ratio depend on how many tokens the query had, not on the intended per-synonym weighting – that's why normalize (default true) happens here, as the final step.\n\nFor each of vec's original nonzero (tokenID, weight) pairs (captured once, before any appending), looks up its string via token(voc, tokenID); if it's a key of synonyms, appends weight * weight_fn(distance) at token2id(voc, synonym) for every (synonym, distance) pair (an OOV synonym – token2id returning 0 – is silently skipped, matching bagofwords!/vectorize!'s existing convention). The appended entries are then merged into vec's existing nonzeros: the combined (nzind, nzval) arrays are heap-sorted by id (reusing SimilaritySearch.heapify!/heapsort!, the same coupled-array sort used to build a SparseVector out of a KnnQueue), duplicate ids (a synonym that was also already present, or reached via two different original tokens) are combined in a single two-pointer reduction pass, and the backing arrays are resize!d down to the final count – an in-place O(n log n) merge, no new allocation for the index/value storage itself.\n\nweight_fn defaults to exp(-d) (1.0 at distance 0, decaying smoothly with distance); pass e.g. d -> d < 0.3 ? 0.5 : 0.0 for a hard cutoff instead.\n\n\n\n\n\n","title":"TextSearch.expand_synonyms!"},{"category":"method","location":"api/#TextSearch.expand_synonyms!-Union{Tuple{V}, Tuple{K}, Tuple{AbstractDict{K, V}, Vocabulary, Any}} where {K<:Integer, V<:Real}","page":"API","text":"expand_synonyms!(bow::AbstractDict{<:Integer,<:Real}, voc::Vocabulary, synonyms) -> bow\n\nExpands a query's bag-of-words IN PLACE by adding every present token's synonyms as extra keys – the BM25InvertedFile counterpart of the SparseVector method above. There is no weight_fn/normalize here: BM25 scoring (bm25score) never reads the query side's frequencies, only which token ids are present (\"query's own frequencies are not used\"), so an injected synonym only needs to make its id present in bow – any positive count works, and an id already present (e.g. the synonym also appears literally in the query) is left untouched rather than overwritten.\n\nAs with the SparseVector method, bow's original keys are snapshotted once (via collect) before any insertion, so newly-added synonym ids are never themselves expanded. An OOV synonym (token2id returning 0) is silently skipped, matching bagofwords!'s existing convention.\n\n\n\n\n\n","title":"TextSearch.expand_synonyms!"},{"category":"method","location":"api/#TextSearch.filter_tokens!-Tuple{Vocabulary, AbstractVector{TokenizedText}}","page":"API","text":"filter_tokens!(voc::Vocabulary, arr::AbstractVector{TokenizedText})\n\nApplies filter_tokens!(voc, text) to every tokenized document in arr.\n\n\n\n\n\n","title":"TextSearch.filter_tokens!"},{"category":"method","location":"api/#TextSearch.filter_tokens!-Tuple{Vocabulary, TokenizedText}","page":"API","text":"filter_tokens!(voc::Vocabulary, text::TokenizedText)\n\nRemoves tokens from a given tokenized text based using the valid vocabulary\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> tks = tokenize(TextConfig(), \"hello unknownword world\");\n\njulia> filter_tokens!(voc, tks); collect(tks)\n[\"hello\", \"world\"]\n\n\n\n\n\n","title":"TextSearch.filter_tokens!"},{"category":"method","location":"api/#TextSearch.filter_tokens-Tuple{Function, VectorModel}","page":"API","text":"filter_tokens(pred::Function, model::VectorModel)\n\nReturns a copy of model reduced to the tokens for which pred(t) is true, where t is a (; id, occs, ndocs, weight, token) named tuple (see also filter_tokens(pred, voc::Vocabulary)).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> vocsize(filter_tokens(t -> t.ndocs >= 2, model))\n1\n\n\n\n\n\n","title":"TextSearch.filter_tokens"},{"category":"method","location":"api/#TextSearch.filter_tokens-Tuple{Function, Vocabulary}","page":"API","text":"filter_tokens(pred::Function, voc::Vocabulary)\n\nReturns a copy of reduced vocabulary based on evaluating pred function for each entry in voc\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> voc2 = filter_tokens(t -> t.ndocs >= 2, voc);\n\njulia> vocsize(voc2)\n1\n\n\n\n\n\n","title":"TextSearch.filter_tokens"},{"category":"method","location":"api/#TextSearch.lemma_clusters-Tuple{Vocabulary, AbstractDatabase}","page":"API","text":"lemma_clusters(voc::Vocabulary, wordvecs::AbstractDatabase;\n               algorithm::Symbol=:fft, num_clusters::Integer=0,\n               selector::Symbol=:most_frequent, dist=Dist.Cosine(),\n               morphology::Symbol=:jaccard, morphology_threshold::Real=0.3,\n               qgram::Integer=2, min_common_prefix::Integer=3,\n               order::Symbol=:morphology_first, semantic_threshold::Real=1.0) -> Dict{String,String}\n\nDerives a token => lemma map by combining two signals:\n\nSemantic clustering of voc's tokens by their embeddings in wordvecs (column t = embedding of token(voc, t), e.g. from LSI.wordvectors), via one of SimilaritySearch's fft/dnet/randsel/multirandsel. num_clusters = 0 defaults to ceil(sqrt(vocsize(voc))).\nMorphological subclustering inside each semantic cluster (morphology, morphology_threshold, qgram – see _morphology_metric), so only tokens that also look alike end up sharing a lemma.\n\nThen one canonical token is elected per group (selector: :most_frequent by default, :shortest, or :shortest_then_most_frequent) and every other member maps to it. The selector also decides seeding order, so it is more consequential than a tie-break: :shortest lets a short misspelling win, and a junk seed fragments the family around it – measured on 143k Spanish tokens, the typo guera seeded a group that swallowed guerra and left guerras stranded. :most_frequent seeds on the form the corpus actually uses, which recovered guerras -> guerra, jugadores -> jugador and concentraciones -> concentracion in the same run. Subclusters of one are left alone. Returns only non-identity entries – a lookup miss means the token is its own lemma.\n\norder decides which signal partitions first:\n\n:morphology_first (default): surface-similar families over the whole vocabulary (made affordable by blocking on the required shared prefix), then semantic_threshold splits a family whose members are far apart in embedding space. Whole conjugations collapse correctly this way (abandona, abandonado, abandonar, abandone, ... -> abandono).\n:semantic_first: the original order – cluster by embedding, then split each cluster by surface similarity. Retained because it is the only order that respects a caller-supplied algorithm/num_clusters, but it fragments inflection families across clusters.\n\nsemantic_threshold is a distance under dist, so with the default cosine it lives on [0, 2]; the default 1.0 was picked by measurement rather than taste. Tightening it does not buy precision – it mostly deletes correct inflections (at 0.9 only 4 of 10 probed inflections survive, against 9 of 10 at 1.0), while loosening it past ~1.05 stops catching anything (the artifacts it legitimately removes are cross-language and truncation pairs such as academic/academia and abstracta/abstract).\n\nStep 2 is what makes the result lemma-shaped rather than topic-shaped: embeddings alone put \"guerra\" next to \"belico\" rather than next to \"guerras\" (measured ~2% morphological pairs on Spanish Wikipedia), while surface similarity alone would happily merge \"casa\" with \"caso\". Pass morphology=:none to recover the purely semantic behaviour, which elects one representative per semantic cluster and is better described as topic representatives than as lemmas.\n\nExample\n\nlemmas = lemma_clusters(voc, wordvectors(lsi))\nlemmas[\"casas\"]   # \"casa\"\n\n\n\n\n\n","title":"TextSearch.lemma_clusters"},{"category":"method","location":"api/#TextSearch.load_profile-Tuple{AbstractString}","page":"API","text":"load_profile(path::AbstractString) -> (; model, synonyms, lemmas, stopword_candidates, encoder)\n\nReads back a profile written by save_profile – path may be either the directory save_profile produced, or a .zip archive of it (see zip_profile); this is auto-detected via isdir(path), and a .zip is read directly from memory, no extraction to disk needed. Reconstructs the Vocabulary (rebuilding token2id from the stored tokens list) and VectorModel, and returns a NamedTuple:\n\nmodel::VectorModel\nsynonyms::Dict{String,Vector{Pair{String,Float32}}} (empty if none saved)\nlemmas::Dict{String,String} (empty if none saved)\nstopword_candidates::Vector{String} (empty if none saved)\nencoder::Union{Nothing,Dict{String,Any}} (nothing if none saved)\n\nmodel/synonyms are ready to pass straight into e.g. TextInvertedFile(model; synonyms, ...).\n\nErrors if the profile's transformation needs Snowball/Languages to reconstruct (a \"snowball\" tagged-union entry) and those packages aren't loaded yet.\n\n\n\n\n\n","title":"TextSearch.load_profile"},{"category":"method","location":"api/#TextSearch.merge_profiles-Tuple{Any}","page":"API","text":"merge_profiles(profiles; doc_freq_threshold=0.5, synonyms_k=0, rrf_k=60)\n    -> (; model, synonyms, lemmas, stopword_candidates, encoder)\n\nMerges several profiles (each a load_profile result) into one, returning exactly the pieces save_profile takes as keywords, so a merged profile is written with\n\np = merge_profiles(load_profile.(paths))\nsave_profile(dir, p.model; p.synonyms, p.lemmas, p.stopword_candidates, p.encoder)\n\nThis is what makes fit's batching usable: batching a large corpus produces one independent profile per batch, and merging folds them back into the single corpus-wide profile.\n\nWhat is exact, and what is not\n\nVocabulary counts and weights are exact. occs/ndocs/trainsize/numtokens are additive across disjoint document batches, and the weighting scheme is recomputed from the merged counters – so the merged IDF is the true corpus-wide IDF, not an average of per-batch ones. This is the main reason to merge rather than to pick one batch.\nSynonyms are a rank-fusion consensus, not a recomputation – each input's distances come from its own embedding space (see _fuse_synonyms). Recomputing them exactly would need the corpus, or a persisted projection, neither of which a profile carries.\nLemmas are a plurality vote over the inputs' clusterings (see _vote_lemmas).\nStopword candidates are recomputed from the merged counters at doc_freq_threshold, then unioned with the inputs' recorded candidates – a token every input already removed is absent from the merged vocabulary and could not be re-derived, but is still a stopword and stays recorded as one.\n\nInputs must share their normalization and tokenization settings, and their weighting scheme; transformations may differ only in their stopword set. EntropyWeighting cannot be merged, since recomputing it needs the labeled corpus.\n\nsynonyms_k = 0 keeps as many neighbors per token as the richest input had.\n\n\n\n\n\n","title":"TextSearch.merge_profiles"},{"category":"method","location":"api/#TextSearch.merge_voc-Tuple{Vocabulary, Vocabulary, Vararg{Any}}","page":"API","text":"merge_voc(voc1::Vocabulary, voc2::Vocabulary[, ...])\nmerge_voc(pred::Function, voc1::Vocabulary, voc2::Vocabulary[, ...])\n\nMerges two or more vocabularies into a new one. A predicate function can be used to filter token entries.\n\nNote: All vocabularies should had been created with a compatible TextConfig to be able to work on them.\n\nExample\n\njulia> cfg = TextConfig();\n\njulia> voc1 = Vocabulary(cfg, [\"hello world\"]; verbose=false);\n\njulia> voc2 = Vocabulary(cfg, [\"hello there\"]; verbose=false);\n\njulia> vocsize(merge_voc(voc1, voc2))\n3\n\n\n\n\n\n","title":"TextSearch.merge_voc"},{"category":"method","location":"api/#TextSearch.ndocs-Tuple{Vocabulary, Integer}","page":"API","text":"ndocs(voc::Vocabulary, tokenID::Integer)\nndocs(voc::Vocabulary)\n\nNumber of documents containing the token tokenID (0 is out-of-vocabulary and yields 0 instead of erroring), or the whole per-token vector when called without a tokenID.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> ndocs(voc, token2id(voc, \"hello\"))\n2\n\n\n\n\n\n","title":"TextSearch.ndocs"},{"category":"method","location":"api/#TextSearch.numtokens-Tuple{Vocabulary}","page":"API","text":"numtokens(voc::Vocabulary)\n\nTotal number of (non-unique) tokens seen while building voc.\n\n\n\n\n\n","title":"TextSearch.numtokens"},{"category":"method","location":"api/#TextSearch.occs-Tuple{Vocabulary, Integer}","page":"API","text":"occs(voc::Vocabulary, tokenID::Integer)\noccs(voc::Vocabulary)\n\nTotal occurrences of the token tokenID across the corpus (0 is out-of-vocabulary and yields 0 instead of erroring), or the whole per-token vector when called without a tokenID.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> occs(voc, token2id(voc, \"hello\"))\n2\n\n\n\n\n\n","title":"TextSearch.occs"},{"category":"method","location":"api/#TextSearch.push_token!-Tuple{Vocabulary, Any, Integer, Integer}","page":"API","text":"push_token!(voc::Vocabulary, token, occs::Integer, ndocs::Integer)\npush_token!(voc::Vocabulary, token; occs::Integer=0, ndocs::Integer=0)\n\nRegisters token in voc if not already present (assigning it a new id), or accumulates occs/ndocs into its existing entry otherwise. Returns the token's id.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), 0, 0);\n\njulia> TextSearch.push_token!(voc, \"cat\"; occs=1, ndocs=1)\n0x00000001\n\n\n\n\n\n","title":"TextSearch.push_token!"},{"category":"method","location":"api/#TextSearch.save_profile-Tuple{AbstractString, VectorModel}","page":"API","text":"save_profile(dir::AbstractString, model::VectorModel;\n             synonyms::AbstractDict=Dict{String,Vector{Pair{String,Float32}}}(),\n             lemmas::AbstractDict{String,String}=Dict{String,String}(),\n             stopword_candidates::AbstractVector{<:AbstractString}=String[],\n             encoder::Union{Nothing,NamedTuple}=nothing) -> dir\n\nSerializes model – its voc's TextConfig and vocabulary counters, its weighting scheme, and its precomputed weight vector – together with a synonyms network (e.g. as produced by LSI.synonyms), a lemmas map (e.g. as produced by lemma_clusters), a stopword_candidates list (e.g. as produced by stopword_candidates), and encoder provenance metadata, into dir (created if missing) as a small directory of plain, human-readable JSON files: one per \"large\" piece – vocabulary.json, weights.json, synonyms.json/lemmas.json/stopword_candidates.json (each only written if non-empty), and stopwords.json (only written if model's transformation involves one) – tied together by a small manifest.json that holds everything else (normalization/tokenization flags, weighting tags, encoder metadata, and file references). Load it back with load_profile, or package it for distribution with zip_profile.\n\nlemmas should map only non-identity tokens (a lookup miss on load means \"token is its own lemma\"). stopword_candidates is distinct from and additive to the stopwords.json mechanism above: that one is the applied IgnoreStopwords set baked into the transformation, this one is the candidate list a frequency-based detector produced for review – a profile can have neither, either, or both (they typically coincide when a detector's candidates were the ones actually wired into the transformation). encoder is a NamedTuple such as (; kind=:lsi, outdim=128, scaling=:none, source_path=\"\") recording which encoder produced synonyms/lemmas and its hyperparameters – for provenance only; the encoder's own projection (e.g. an LSI P matrix) is not persisted.\n\nDeliberately NOT a generic object-graph dump (unlike e.g. JLD2): every field is encoded by hand into a small, versioned schema, so every file is fully inspectable/diffable/portable and there is nothing pointer- or code-shaped to accidentally serialize.\n\nA TokenizationConfig with custom (non-empty) generators, or a transformation that isn't IdentityTokenTransformation/IgnoreStopwords/SnowballTokenTransformation/ ChainTransformation of those, errors clearly rather than silently mis-saving.\n\n\n\n\n\n","title":"TextSearch.save_profile"},{"category":"method","location":"api/#TextSearch.sparse_coo-Union{Tuple{AbstractVector{S}}, Tuple{S}, Tuple{AbstractVector{S}, Any}} where S<:Dict","page":"API","text":"sparse(cols::AbstractVector{<:Dict}, m=0; minweight=1e-9) \nsparse_coo(cols::AbstractVector{<:Dict}, minweight=1e-9)\n\nCreates a sparse matrix from an array of Dict sparse vectors.\n\nExample\n\njulia> cols = [Dict{UInt32,Float32}(1 => 0.5), Dict{UInt32,Float32}(2 => 0.8)];\n\njulia> Matrix(sparse(cols))\n2×2 Matrix{Float32}:\n 0.5  0.0\n 0.0  0.8\n\n\n\n\n\n","title":"TextSearch.sparse_coo"},{"category":"method","location":"api/#TextSearch.sparsedot-Union{Tuple{Ti}, Tuple{Tv}, Tuple{SparseArrays.SparseVector{Tv, Ti}, SparseArrays.SparseVector{Tv, Ti}}} where {Tv, Ti}","page":"API","text":"sparsedot(a::SparseVector, b::SparseVector; small_threshold::Int=30, ratio_threshold::Float64=3.0)\n\nAdaptive dot product between two SparseVectors:\n\nboth sides have fewer than small_threshold stored entries, or their sizes are within ratio_threshold of each other: a plain linear merge (the same algorithm LinearAlgebra.dot already uses for SparseVector).\notherwise (one side much larger than the other — e.g. a short query against a long document): a Hwang-Lin/galloping merge — for each stored entry of the smaller side, an exponential (\"galloping\") search with memory of the last found position locates its match in the larger side in O(log gap) instead of a full linear scan.\n\nThis is deliberately not a method of LinearAlgebra.dot(::SparseVector,::SparseVector): SparseArrays already owns that method (a plain merge), and shadowing it package-wide would be a much more aggressive form of type piracy than TextSearch's existing Dict overloads of dot/normalize! (neither SparseVector nor dot belong to TextSearch, whereas extending dot for Dict doesn't collide with any other package's definitions). evaluate for SparseVector uses sparsedot internally.\n\nExample\n\njulia> using SparseArrays\n\njulia> sparsedot(sparsevec(UInt32[1, 2], Float32[0.6, 0.8], 10), sparsevec(UInt32[2], Float32[1.0], 10))\n0.8f0\n\n\n\n\n\n","title":"TextSearch.sparsedot"},{"category":"function","location":"api/#TextSearch.stopword_candidates","page":"API","text":"stopword_candidates(voc::Vocabulary, threshold::Real=0.5) -> Vector{String}\nstopword_candidates(model::VectorModel, threshold::Real=0.5) -> Vector{String}\n\nFlags tokens whose document-frequency ratio ndocs(voc, id) / trainsize(voc) exceeds threshold as stopword candidates, sorted by decreasing ratio (most extreme first). A frequency heuristic only – it does not inspect token semantics – so results should be reviewed before being wired into an IgnoreStopwords transformation.\n\nExample\n\ncandidates = stopword_candidates(voc, 0.5)\ntextconfig = TextConfig(voc.textconfig; transformation=IgnoreStopwords(Set(candidates)))\n\n\n\n\n\n","title":"TextSearch.stopword_candidates"},{"category":"method","location":"api/#TextSearch.table-Tuple{VectorModel, Any}","page":"API","text":"table(model::VectorModel, TableConstructor)\n\nBuilds a Tables.jl-compatible table (e.g., a DataFrame) with one row per token, using TableConstructor (e.g. DataFrame) as the row-table constructor. Columns are token, ndocs, occs, and weight.\n\nExample\n\njulia> using DataFrames\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> table(model, DataFrame)\n6×4 DataFrame\n Row │ token   ndocs  occs   weight\n     │ String  Int32  Int32  Float32\n─────┼────────────────────────────────\n   1 │ hello       2      2  0.485427\n   2 │ world       1      1  1.22239\n   3 │ there       1      1  1.22239\n   4 │ the         1      1  1.22239\n   5 │ cat         1      1  1.22239\n   6 │ sat         1      1  1.22239\n\n\n\n\n\n","title":"TextSearch.table"},{"category":"method","location":"api/#TextSearch.table-Tuple{Vocabulary, Any}","page":"API","text":"table(voc::Vocabulary, TableConstructor)\n\nBuilds a Tables.jl-compatible table (e.g., a DataFrame) with one row per token, using TableConstructor (e.g. DataFrame) as the row-table constructor. Columns are token, ndocs, and occs.\n\nExample\n\njulia> using DataFrames\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> table(voc, DataFrame)\n6×3 DataFrame\n Row │ token   ndocs  occs\n     │ String  Int32  Int32\n─────┼──────────────────────\n   1 │ hello       2      2\n   2 │ world       1      1\n   3 │ there       1      1\n   4 │ the         1      1\n   5 │ cat         1      1\n   6 │ sat         1      1\n\n\n\n\n\n","title":"TextSearch.table"},{"category":"method","location":"api/#TextSearch.token-Tuple{Vocabulary, Integer}","page":"API","text":"token(voc::Vocabulary, tokenID::Integer)\ntoken(voc::Vocabulary)\n\nThe token string for tokenID (0 is out-of-vocabulary and yields \"\" instead of erroring), or the whole token vector when called without a tokenID.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> token(voc, token2id(voc, \"hello\"))\n\"hello\"\n\n\n\n\n\n","title":"TextSearch.token"},{"category":"method","location":"api/#TextSearch.token2id-Tuple{Vocabulary, AbstractString}","page":"API","text":"token2id(voc::Vocabulary, tok::AbstractString)::UInt32\n\nLooks up the id of tok in voc; returns 0 when tok is out of vocabulary.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> token2id(voc, \"hello\")\n0x00000001\n\njulia> token2id(voc, \"unknown\")\n0x00000000\n\n\n\n\n\n","title":"TextSearch.token2id"},{"category":"method","location":"api/#TextSearch.tokenize_and_append!-Tuple{Vocabulary, Any}","page":"API","text":"tokenize_and_append!(voc::Vocabulary, corpus; isnormalized::Bool=false)\n\nParse each document in the given corpus and appends each token to the vocabulary.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), 0, 0);\n\njulia> tokenize_and_append!(voc, [\"hello world\", \"hello there\"]);\n\njulia> vocsize(voc)\n3\n\n\n\n\n\n","title":"TextSearch.tokenize_and_append!"},{"category":"method","location":"api/#TextSearch.trainsize-Tuple{Vocabulary}","page":"API","text":"trainsize(voc::Vocabulary)\n\nNumber of documents used to build voc.\n\n\n\n\n\n","title":"TextSearch.trainsize"},{"category":"method","location":"api/#TextSearch.update_voc!-Tuple{Vocabulary, Vocabulary}","page":"API","text":"update_voc!(voc::Vocabulary, another::Vocabulary)\nupdate_voc!(pred::Function, voc::Vocabulary, another::Vocabulary)\n\nUpdate voc vocabulary using another vocabulary. Optionally a predicate can be given to filter vocabularies.\n\nNote 1: corpuslen remains unchanged (the structure is immutable and a new Vocabulary should be created to update this field). Note 2: Both voc and another vocabularies should had been created with a compatible TextConfig to be able to work on them.\n\nExample\n\njulia> cfg = TextConfig();\n\njulia> voc = Vocabulary(cfg, 0, 0);\n\njulia> update_voc!(voc, Vocabulary(cfg, [\"hello world\"]; verbose=false));\n\njulia> vocsize(voc)\n2\n\n\n\n\n\n","title":"TextSearch.update_voc!"},{"category":"method","location":"api/#TextSearch.vectorize!-Tuple{TextSearch.VectorizeBuffer, VectorModel, Any}","page":"API","text":"vectorize!(buff::VectorizeBuffer, model::VectorModel, text; normalize=true, minweight=1e-6, isnormalized::Bool=false)\n\nTokenizes text and weights it using model's local/global weighting scheme, returning the result as a SparseVector{Float32,Int32}; entries with a weight below minweight are dropped, and the result is L2-normalized unless normalize=false. buff is used as scratch space (see VectorizeBuffer; tokenization scratch space is borrowed separately via tokenizerbuffer). See vectorize for a version that manages the scratch buffer for you.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> buff = TextSearch.VectorizeBuffer();\n\njulia> TextSearch.vectorize!(buff, model, \"hello world\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.369076\n  [2]  =  0.929399\n\n\n\n\n\n","title":"TextSearch.vectorize!"},{"category":"method","location":"api/#TextSearch.vectorize-Tuple{VectorModel, Any}","page":"API","text":"vectorize(model::VectorModel, text; normalize=true, minweight=1e-6, isnormalized::Bool=false)\n\nComputes the weighted sparse vector (a SparseVector{Float32,Int32}) representation of text under model. text can be a string or a list of strings (a multi-field document).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> vectorize(model, \"hello world\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.369076\n  [2]  =  0.929399\n\n\n\n\n\n","title":"TextSearch.vectorize"},{"category":"method","location":"api/#TextSearch.vectorize_corpus-Tuple{VectorModel, Any}","page":"API","text":"vectorize_corpus(model::VectorModel, corpus; normalize=true, minweight=1e-6, isnormalized::Bool=false, verbose=true)\n\nComputes the vectorize representation of every document in corpus, processed in parallel across threads (the batch size is picked automatically from the size of corpus, as in SimilaritySearch.getminbatch).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> vectorize_corpus(model, corpus; verbose=false)[1]\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.369076\n  [2]  =  0.929399\n\n\n\n\n\n","title":"TextSearch.vectorize_corpus"},{"category":"method","location":"api/#TextSearch.vocabulary_from_thesaurus-Tuple{TextConfig, AbstractVector}","page":"API","text":"vocabulary_from_thesaurus(textconfig::TextConfig, tokens::AbstractVector)\n\nCreates a Vocabulary directly from a list of tokens (a thesaurus), instead of tokenizing a corpus; every token is registered with occs=1 and ndocs=1.\n\nExample\n\njulia> voc = vocabulary_from_thesaurus(TextConfig(), [\"cat\", \"dog\", \"bird\"]);\n\njulia> vocsize(voc)\n3\n\njulia> token2id(voc, \"cat\")\n0x00000001\n\n\n\n\n\n","title":"TextSearch.vocabulary_from_thesaurus"},{"category":"method","location":"api/#TextSearch.vocsize-Tuple{Vocabulary}","page":"API","text":"vocsize(voc::Vocabulary)\n\nNumber of unique tokens in voc.\n\n\n\n\n\n","title":"TextSearch.vocsize"},{"category":"function","location":"api/#TextSearch.zip_profile","page":"API","text":"zip_profile(dir::AbstractString, zippath::AbstractString=dir * \".zip\") -> zippath\n\nPackages a profile directory (as written by save_profile) into a single .zip archive at zippath, ready to distribute as one file. load_profile reads a .zip produced this way directly (no extraction needed).\n\n\n\n\n\n","title":"TextSearch.zip_profile"},{"category":"type","location":"api/#TextSearch.BOW","page":"API","text":"BOW = Dict{UInt32,Int32}\n\nA bag of words: a sparse token id => occurrence count mapping for a single document, as produced by bagofwords/bagofwords!.\n\nExample\n\njulia> BOW(0x00000001 => 2, 0x00000002 => 1)\nDict{UInt32, Int32}(0x00000002 => 1, 0x00000001 => 2)\n\n\n\n\n\n","title":"TextSearch.BOW"},{"category":"type","location":"api/#TextSearch.BinaryGlobalWeighting","page":"API","text":"BinaryGlobalWeighting()\n\nThe weight is 1 for known tokens, 0 for out of vocabulary tokens\n\n\n\n\n\n","title":"TextSearch.BinaryGlobalWeighting"},{"category":"type","location":"api/#TextSearch.BinaryLocalWeighting","page":"API","text":"BinaryLocalWeighting()\n\nThe weight is 1 for known tokens, 0 for out of vocabulary tokens\n\n\n\n\n\n","title":"TextSearch.BinaryLocalWeighting"},{"category":"type","location":"api/#TextSearch.CombineWeighting","page":"API","text":"CombineWeighting\n\nAbstract type for the strategies that turn a per-token, per-class distribution and its empirical entropy into a single global weight (via the internal combine_weight function), used by EntropyWeighting. Available strategies: NormalizedEntropy and SigmoidPenalizeFewSamples.\n\n\n\n\n\n","title":"TextSearch.CombineWeighting"},{"category":"type","location":"api/#TextSearch.EntropyWeighting","page":"API","text":"EntropyWeighting()\n\nA GlobalWeighting that scores each token by the empirical entropy of its occurrences across document classes/labels, instead of the plain document frequency used by IdfWeighting — tokens whose occurrences concentrate on few classes get a higher weight than tokens spread uniformly across all classes. Since it needs document labels, it is not built via the generic VectorModel(gw, lw, voc) constructor; use VectorModel(::EntropyWeighting, lw, voc, corpus, labels) instead.\n\nExample\n\njulia> corpus = [\"me gusta\", \"me encanta\", \"no me gusta\", \"odio esto\"];\n\njulia> labels = [\"pos\", \"pos\", \"neg\", \"neg\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(EntropyWeighting(), TfWeighting(), voc, corpus, labels; verbose=false);\n\njulia> vectorize(model, \"me gusta\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 1 stored entry:\n  [1]  =  1.0\n\n\n\n\n\n","title":"TextSearch.EntropyWeighting"},{"category":"type","location":"api/#TextSearch.FreqWeighting","page":"API","text":"FreqWeighting()\n\nFrequency weighting\n\n\n\n\n\n","title":"TextSearch.FreqWeighting"},{"category":"type","location":"api/#TextSearch.GlobalWeighting","page":"API","text":"GlobalWeighting\n\nAbstract type for global weighting\n\n\n\n\n\n","title":"TextSearch.GlobalWeighting"},{"category":"type","location":"api/#TextSearch.IdIntWeight","page":"API","text":"IdIntWeight(id, weight)\n\nStores a pair of objects to be accessed. Similar to IdWeight but it stores an integer weight\n\n\n\n\n\n","title":"TextSearch.IdIntWeight"},{"category":"type","location":"api/#TextSearch.IdWeight","page":"API","text":"IdWeight(id, weight)\n\nStores a pair of entries of the posting lists\n\n\n\n\n\n","title":"TextSearch.IdWeight"},{"category":"type","location":"api/#TextSearch.IdfWeighting","page":"API","text":"IdfWeighting()\n\nInverse document frequency weighting\n\n\n\n\n\n","title":"TextSearch.IdfWeighting"},{"category":"type","location":"api/#TextSearch.LocalWeighting","page":"API","text":"LocalWeighting\n\nAbstract type for local weighting\n\n\n\n\n\n","title":"TextSearch.LocalWeighting"},{"category":"type","location":"api/#TextSearch.NormalizedEntropy","page":"API","text":"NormalizedEntropy()\n\nA CombineWeighting that weights a token as 1 - entropy / maxent: tokens that discriminate well between classes (low entropy) get a weight close to 1, tokens spread uniformly across classes (entropy close to maxent) get a weight close to 0.\n\nExample\n\njulia> corpus = [\"me gusta\", \"me encanta\", \"no me gusta\", \"odio esto\"];\n\njulia> labels = [\"pos\", \"pos\", \"neg\", \"neg\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(EntropyWeighting(), TfWeighting(), voc, corpus, labels; mindocs=1, comb=NormalizedEntropy(), verbose=false);\n\njulia> vectorize(model, \"me gusta\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.9996127\n  [2]  =  0.0278291\n\n\n\n\n\n","title":"TextSearch.NormalizedEntropy"},{"category":"type","location":"api/#TextSearch.SigmoidPenalizeFewSamples","page":"API","text":"SigmoidPenalizeFewSamples()\n\nLike NormalizedEntropy, but additionally down-weights tokens seen in very few documents (low ndocs) via a sigmoid-like penalty on log2(ndocs), so that rare tokens don't get an unduly high weight just because they appear in a single class.\n\nExample\n\njulia> corpus = [\"me gusta\", \"me encanta\", \"no me gusta\", \"odio esto\"];\n\njulia> labels = [\"pos\", \"pos\", \"neg\", \"neg\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(EntropyWeighting(), TfWeighting(), voc, corpus, labels; mindocs=1, comb=SigmoidPenalizeFewSamples(), verbose=false);\n\njulia> vectorize(model, \"me gusta\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.99974245\n  [2]  =  0.02269659\n\n\n\n\n\n","title":"TextSearch.SigmoidPenalizeFewSamples"},{"category":"type","location":"api/#TextSearch.TextModel","page":"API","text":"TextModel\n\nAbstract type for text-to-vector weighting models (see VectorModel).\n\n\n\n\n\n","title":"TextSearch.TextModel"},{"category":"type","location":"api/#TextSearch.TfWeighting","page":"API","text":"TfWeighting()\n\nTerm frequency weighting\n\n\n\n\n\n","title":"TextSearch.TfWeighting"},{"category":"type","location":"api/#TextSearch.TpWeighting","page":"API","text":"TpWeighting()\n\nTerm probability weighting\n\n\n\n\n\n","title":"TextSearch.TpWeighting"},{"category":"type","location":"api/#TextSearch.VectorModel","page":"API","text":"VectorModel{_G<:GlobalWeighting, _L<:LocalWeighting}\n\nCombines a Vocabulary with a local/global term-weighting scheme (e.g. TfWeighting+IdfWeighting for classical TF-IDF) to turn bags of words into weighted sparse vectors (SparseVector{Float32,Int32}) via vectorize/vectorize!. Build one with VectorModel(gw, lw, voc).\n\nFields\n\nglobal_weighting: the GlobalWeighting scheme (e.g. IDF), applied per-token, corpus-wide.\nlocal_weighting: the LocalWeighting scheme (e.g. TF), applied per-token, per-document.\nvoc: the underlying Vocabulary.\nmaxoccs: maximum per-token occurrence count in voc, used by some local weightings.\nweight: precomputed per-token global weight (weight[tokenID]).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> vectorize(model, \"hello world\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.369076\n  [2]  =  0.929399\n\n\n\n\n\n","title":"TextSearch.VectorModel"},{"category":"method","location":"api/#TextSearch.VectorModel-Tuple{EntropyWeighting, LocalWeighting, Vocabulary, AbstractVector, AbstractVector}","page":"API","text":"VectorModel(ent::EntropyWeighting, lw::LocalWeighting, voc::Vocabulary, corpus::AbstractVector, labels::AbstractVector;\n    mindocs=3,\n    smooth=3,\n    weights=:balance,\n    comb::CombineWeighting=NormalizedEntropy(),\n    verbose=true\n)\n\nCreates a VectorModel with EntropyWeighting as its global weighting scheme. Unlike the generic VectorModel(gw, lw, voc) constructor, this one needs the actual corpus and a matching labels vector (one label per document) to compute, for each token, its occurrence distribution across classes and the resulting entropy-based weight.\n\nmindocs: tokens occurring in fewer than mindocs documents get weight 0.\nsmooth: additive (Laplace-like) smoothing applied to the per-class occurrence counts before computing entropy, to avoid zero counts.\nweights: how to reweight classes before computing entropy — :balance compensates for class-size imbalance, :none (or nothing) leaves classes unweighted, or pass an AbstractVector of per-class weights directly.\ncomb: the CombineWeighting strategy combining entropy and evidence into the final per-token weight.\n\nExample\n\njulia> corpus = [\"me gusta\", \"me encanta\", \"no me gusta\", \"odio esto\"];\n\njulia> labels = [\"pos\", \"pos\", \"neg\", \"neg\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(EntropyWeighting(), TfWeighting(), voc, corpus, labels; mindocs=1, verbose=false);\n\njulia> vectorize(model, \"me gusta\")\n6-element SparseArrays.SparseVector{Float32, Int32} with 2 stored entries:\n  [1]  =  0.9996127\n  [2]  =  0.0278291\n\n\n\n\n\n","title":"TextSearch.VectorModel"},{"category":"method","location":"api/#TextSearch.VectorModel-Tuple{GlobalWeighting, LocalWeighting, Vocabulary}","page":"API","text":"VectorModel(gw::GlobalWeighting, lw::LocalWeighting, voc::Vocabulary; weight=nothing)\n\nCreates a VectorModel for the given vocabulary voc using the local weighting lw (e.g. TfWeighting) and global weighting gw (e.g. IdfWeighting). The per-token global weight vector is computed from voc unless weight is given explicitly (e.g. when reusing weights computed elsewhere, such as EntropyWeighting).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> model = VectorModel(IdfWeighting(), TfWeighting(), voc);\n\njulia> length(model.weight)\n6\n\n\n\n\n\n","title":"TextSearch.VectorModel"},{"category":"type","location":"api/#TextSearch.VectorizeBuffer","page":"API","text":"VectorizeBuffer(n=128)\n\nPooled per-thread scratch space for vectorize!: ids accumulates every in-vocabulary token id seen in a document (with repeats), which is then sorted and run-length-encoded to recover per-token occurrence counts — the same merge-based strategy sum(::AbstractVector{<:SparseVector}) uses, avoiding the Dict allocation/hashing a BOW would need for this per-call, performance-sensitive path.\n\n\n\n\n\n","title":"TextSearch.VectorizeBuffer"},{"category":"type","location":"api/#TextSearch.Vocabulary","page":"API","text":"Vocabulary\n\nHolds the token ⇄ id mapping produced while parsing a corpus, along with per-token occurrence and document-frequency counters. A Vocabulary is the entry point of the processing pipeline: it is built from a TextConfig and a corpus, and is later consumed by VectorModel, BM25Scorer, and bagofwords.\n\nFields\n\ntextconfig: the TextConfig used to tokenize the corpus that produced this vocabulary.\ntoken: id -> token string table.\noccs: id -> total number of occurrences of the token across the corpus.\nndocs: id -> number of documents containing the token.\ntoken2id: token -> id reverse mapping (0 means \"unknown token\").\ntrainsize: number of documents used to build the vocabulary.\nnumtokens: total number of (non-unique) tokens seen while building the vocabulary.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> vocsize(voc)\n3\n\njulia> token2id(voc, \"hello\")\n0x00000001\n\n\n\n\n\n","title":"TextSearch.Vocabulary"},{"category":"method","location":"api/#TextSearch.Vocabulary-Tuple{TextConfig, Any}","page":"API","text":"Vocabulary(textconfig::TextConfig, corpus; buffsize=2^16, isnormalized::Bool=false, verbose=true)\n\nTokenizes corpus under textconfig and builds the resulting Vocabulary. corpus can be any vector of documents (each document a string or a list of strings) or an iterable/generator of documents (useful for corpora too large to fit in memory); in the generator case, documents are consumed and tokenized in batches of buffsize.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> vocsize(voc), trainsize(voc)\n(3, 2)\n\n\n\n\n\n","title":"TextSearch.Vocabulary"},{"category":"method","location":"api/#TextSearch.Vocabulary-Tuple{TextConfig, Int64, Int64}","page":"API","text":"Vocabulary(textconfig::TextConfig, trainsize::Int, numtokens::Int)\n\nCreates an empty Vocabulary (no tokens registered yet) preallocated with capacity hints based on trainsize (following Heaps' law). trainsize and numtokens may be 0 when unknown ahead of time; use push_token! or tokenize_and_append! to fill it, or use Vocabulary(textconfig, corpus) to build it directly from a corpus.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), 0, 0);\n\njulia> TextSearch.push_token!(voc, \"cat\"; occs=1, ndocs=1)\n0x00000001\n\njulia> vocsize(voc)\n1\n\n\n\n\n\n","title":"TextSearch.Vocabulary"},{"category":"method","location":"api/#SimilaritySearch.Intersections._remove_empty!-Tuple{Any, Any}","page":"API","text":"_remove_empty!(L, P)\n\nInplace removal of empty lists\n\n\n\n\n\n","title":"SimilaritySearch.Intersections._remove_empty!"},{"category":"method","location":"api/#SimilaritySearch.Intersections._sort!-Tuple{Any, Vector}","page":"API","text":"_sort!(L, P)\n\nAdaptive bubble sort, efficient than other approaches because we expect a few sets and almost sorted\n\n\n\n\n\n","title":"SimilaritySearch.Intersections._sort!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.binarysearch","page":"API","text":"binarysearch(A, x, sp=1, ep=length(A))\n\nFinds the insertion position of x in A in the range sp:ep\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.binarysearch"},{"category":"function","location":"api/#SimilaritySearch.Intersections.bk!","page":"API","text":"bk!(output, L, P, findpos::Function=doublingsearch) -> int. size\n\nComputes the intersection of a list of posting lists using the Barbay and Kenyon algorithm.\n\nSee umerge! if you need to change how matches are captured (onmatch! function).\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.bk!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.bkt!","page":"API","text":"bkt!(output, L, [P,], [findpos::Function=doublingsearch]; t::Int=length(L)) -> num. matches\n\nBarybay & Kenyon t-thresholds \n\nSee umerge! if you need to change how matches are captured (onmatch! function).\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.bkt!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.doublingsearch","page":"API","text":"doublingsearch(A, x, sp=1, ep=length(A))\n\nFinds the insertion position of x in A, starting at sp\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.doublingsearch"},{"category":"function","location":"api/#SimilaritySearch.Intersections.doublingsearchrev","page":"API","text":"doublingsearchrev(A, x, sp=1, ep=length(A))\n\nFinds the insertion position of x in A, starting at the end\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.doublingsearchrev"},{"category":"method","location":"api/#SimilaritySearch.Intersections.imerge2!-Tuple{Any, Any, Any}","page":"API","text":"imerge2!(output, A, B) -> num. matches\n\nComputes the intersection of A and B (sorted arrays), using a merge-like algorithm, and stores it in output or calls onmatch(A, i, B, j) on every match. See bk! for how change the behaviour of matches.\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.imerge2!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.seqsearch","page":"API","text":"seqsearch(A, x, sp=1, ep=length(A))\n\nSequential search, i.e., it starts from sp to ep\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.seqsearch"},{"category":"function","location":"api/#SimilaritySearch.Intersections.seqsearchrev","page":"API","text":"seqsearchrev(A, x, sp=1, ep=length(A))\n\nReverse sequential search, i.e., it starts from ep to sp\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.seqsearchrev"},{"category":"function","location":"api/#SimilaritySearch.Intersections.svs_!","page":"API","text":"svs(postinglists, intersect2=baezayates!) -> output\n\nComputes the intersection of the ordered lists in postinglists using a small vs small strategy. Accepts an intersection algorithm of two sets.\n\nThis method does not give explicit support for onmatch2!\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.svs_!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.umerge!","page":"API","text":" umerge!(output, L, P=ones(Int32, length(L_)); t::Int=1) -> num. matches\n\nMerges posting lists in L and saves the union in output. The merge result is stored into output array. You can customize how to do this specializing the onmatch!(output, L, P, t::Int) function.\n\nArguments:\n\nL: The array of posting lists, the array can be destroyed in the process.\nP: The array of current positions in posting lists, i.e., initial state as an array of ones of size L.\nt: Computes t-thresholds, i.e., t from 1 (union) to |L| (intersection) of posting lists in L using findpos storing the result set in output.\n\nAbout the callback function\n\noutput, L and P are the arguments same than the input, while t is the actual number of lists having the match. Note 1: you should access L[i][P[i]] to get the entry of the ith list, i.e., 1 leq t leq P. Note 2: L and P as container lists will be also modified, the contained lists remain untouched.\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.umerge!"},{"category":"function","location":"api/#SimilaritySearch.Intersections.xmerge!","page":"API","text":"xmerge!(output, L, P=ones(Int32, length(L_)); t::Int=1) -> num. matches\n\nSolves t-threshold set operation using other algorithms choosing among them by given t\n\nArguments:\n\noutput: vector like to store the t-threshold set\nL: the list of posting lists to be merged. The posting lists are left untouched but the container is modified.\nP: indices of the current merging-state (idem to L)\nt: the threshold, i.e., t=1 (union) ... t=|L| performs intersection)\n\nSimple wrapper around other specific operations depending on t value\n\nSee umerge! if you need to modify the output behaviour.\n\n\n\n\n\n","title":"SimilaritySearch.Intersections.xmerge!"},{"category":"method","location":"api/#Base.length-Tuple{AbstractInvertedFile}","page":"API","text":"length(idx::AbstractInvertedFile)\n\nNumber of indexed elements\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.convertident-Tuple{Pair}","page":"API","text":"convertident(u)\n\nConverts an element of an identiterator fallback into a plain key/id, discarding any paired weight if present as a Pair.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.convertident"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.has_exact_fastpath-Tuple{SimilaritySearch.Dist.Sets.Intersection}","page":"API","text":"has_exact_fastpath(dist::PreMetric)::Bool\n\nWhether the score computed while merging posting lists (via set_distance_evaluate) is already the exact dist value. When false, search_invfile instead evaluates dist directly against the objects stored in the index's db for every merge candidate — see FallbackInvFileOutput in invfilesearch.jl; raise t above the default 1 to bound how many such evaluations happen per query.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.has_exact_fastpath"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.identiterator-Tuple{Distances.PreMetric, Any}","page":"API","text":"identiterator(dist::PreMetric, obj)\n\nDistance-aware id-only iterator for obj. Defaults to the distance-agnostic identiterator(obj) dispatch tree above; overload this for a specific (DistType, ObjType) pair when the same native object type must generate different candidate ids depending on which distance the enclosing index is built for (e.g. a shingle-based candidate encoding for a sequence distance).\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.identiterator"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.identiterator-Tuple{SparseArrays.SparseVector}","page":"API","text":"identiterator(obj)\n\nIterator over the plain ids/keys in obj, for callers that only need to know which ids/keys are present (e.g. InvertedFile building/re-sorting/searching its posting lists, which never need a weight: the handful of distances with an exact fast path score from intersection size and set sizes alone, and any other distance is evaluated directly against the full objects kept in db – see InvertedFile). Dense Vectors are not accepted directly – convert to a SparseVector first (e.g. via SparseArrays.sparse) so the reduction to non-zero components is explicit in the caller's code.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.identiterator"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.search_invfile-Union{Tuple{PostType}, Tuple{InvertedFile{<:Any, <:SimilaritySearch.AbstractAdjList{UInt32}}, InvertedFileContext, Any, Vector{PostType}, SimilaritySearch.PQueue.AbstractKnnQueue, Any}} where PostType<:SimilaritySearch.InvertedFiles.PostingList","page":"API","text":"search_invfile(idx::InvertedFile, ctx::InvertedFileContext, q, Q, res::AbstractKnnQueue, t)\n\nFind candidates for solving query Q using idx. It calls callback on each candidate (objID, dist)\n\nArguments\n\nidx: inverted index\nq: the query object, only used for distances without an exact fast path (see InvertedFiles.has_exact_fastpath)\nQ: the set of involved posting lists, see select_posting_lists\nt: threshold (t=1 union, t > 1 solves the t-threshold problem); for distances without an exact fast path, t also bounds how many real evaluate calls happen per query — raise it to reduce cost.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.search_invfile"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.select_posting_lists-Tuple{AbstractInvertedFile, InvertedFileContext, Any}","page":"API","text":"select_posting_lists(idx::AbstractInvertedFile, ctx::InvertedFileContext, q)\n\nFetches and prepares the involved posting lists to solve q\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.select_posting_lists"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.set_distance_evaluate-Tuple{SimilaritySearch.Dist.Sets.Intersection, Int32, Int32, Int32}","page":"API","text":"set_distance_evaluate(dist::PreMetric, intersection::Integer, size1::Integer, size2::Integer)\n\nComputes a score for a candidate found while merging posting lists, given the intersection size of the matching posting lists and the total number of non-zero entries of each of the two compared elements (size1, size2). Only defined for the handful of distances with an exact closed form (see has_exact_fastpath) — the resulting value is exact, computed purely from these three integers, with no need to touch the original objects. For any other dist, search_invfile does not call this function at all — it evaluates dist directly against the stored objects for every merge candidate instead (see FallbackInvFileOutput in invfilesearch.jl); use t > 1 to bound how many such evaluations happen per query.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.set_distance_evaluate"},{"category":"method","location":"api/#SimilaritySearch.InvertedFiles.sort_postinglist!-Tuple{SimilaritySearch.AbstractAdjList{UInt32}, Any}","page":"API","text":"sort_postinglist!(adj::AbstractAdjList, N)\n\nSorts a single posting list N (as returned by neighbors(adj, tokenID)) back into the order the merge/search algorithms rely on: ascending by id, for plain token adjacency (UInt32). Override for a different concrete adjacency element type (e.g. a compressed encoding).\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.sort_postinglist!"},{"category":"function","location":"api/#SimilaritySearch.append_items!","page":"API","text":"append_items!(idx, ctx, items)\n\nAppends all items elements into the index idx. It work in parallel using all available threads.\n\nArguments:\n\nidx: The inverted index\nitems: The database of sparse objects, it can be only indices if each object is a list of integers or a set of integers,   SparseVectors, among other combinations (see identiterator for the exact set of   natively supported object types; dense vectors are not accepted directly — convert with   SparseArrays.sparse first).\nn: The number of items to insert (defaults to all)\n\n\n\n\n\n","title":"SimilaritySearch.append_items!"},{"category":"function","location":"api/#SimilaritySearch.push_item!","page":"API","text":"push_item!(idx::AbstractInvertedFile, ctx::InvertedFileContext, obj)\n\nInserts a single element into the index. This operation is not thread-safe.\n\nArguments\n\nidx: The inverted index\nctx: the index's context\nobj: The object to be indexed\n\n\n\n\n\n","title":"SimilaritySearch.push_item!"},{"category":"method","location":"api/#SimilaritySearch.search-Tuple{AbstractInvertedFile, InvertedFileContext, Any, SimilaritySearch.PQueue.AbstractKnnQueue}","page":"API","text":"search(idx::AbstractInvertedFile, ctx::InvertedFileContext, q, res::AbstractKnnQueue; t=1)\n\nSearches q in idx using the cosine dissimilarity, it computes the full operation on idx. res specify the query\n\n\n\n\n\n","title":"SimilaritySearch.search"},{"category":"type","location":"api/#SimilaritySearch.InvertedFiles.AbstractInvertedFile","page":"API","text":"abstract type AbstractInvertedFile <: AbstractSearchIndex end\n\nAbstract inverted file; the concrete data structure is InvertedFile.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.AbstractInvertedFile"},{"category":"type","location":"api/#SimilaritySearch.InvertedFiles.DictInvertedFile","page":"API","text":"const DictInvertedFile{DistType, KeyType, DbType} = InvertedFile{DistType, AdjDict{KeyType, UInt32}, DbType}\n\nA dictionary-backed inverted file mapping posting list keys of type KeyType (e.g., String, Vector{UInt8}, NTuple, Int) to document identifiers (UInt32). Empty or non-existent posting lists are never stored in memory or disk, enabling use over arbitrary or massive key spaces.\n\nConstructors\n\nDictInvertedFile(::Type{KeyType}, dist::PreMetric=Dist.Sets.Jaccard(); db::AbstractDatabase=VectorDatabase(Any[]), hint_size::Integer=0)\nDictInvertedFile(dist::PreMetric=Dist.Sets.Jaccard(); KeyType::Type=Any, db::AbstractDatabase=VectorDatabase(Any[]), hint_size::Integer=0)\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.DictInvertedFile"},{"category":"type","location":"api/#SimilaritySearch.InvertedFiles.InvertedFile","page":"API","text":"InvertedFile(vocsize::Integer, dist::PreMetric=Dist.Sets.Jaccard(); db::AbstractDatabase=VectorDatabase(Any[]))\n\nCreates an empty InvertedFile with plain token/set-membership posting lists (AdjType's element type is UInt32), for the given vocabulary size and distance function dist (typically one of the set metrics in Dist.Sets, e.g. Jaccard, Dice, Intersection, CosineSet, RogersTanimoto; or any other PreMetric — e.g. Dist.NormCosine() for sparse-vector/MIPS-style cosine search — via the generic direct-evaluate fallback).\n\nArguments\n\nvocsize: the vocabulary size of the index\ndist: the distance function to be used in searches\n\nKeyword arguments\n\ndb: the database that will receive a copy of every indexed object (must support push_item!/append_items! for incremental construction, e.g. a VectorDatabase); defaults to an empty, untyped VectorDatabase.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.InvertedFile"},{"category":"type","location":"api/#SimilaritySearch.InvertedFiles.InvertedFile-2","page":"API","text":"struct InvertedFile{DistType<:PreMetric, AdjType<:AbstractAdjList, DbType<:AbstractDatabase} <: AbstractInvertedFile\n\nA general-purpose inverted index: a sparse matrix-like representation mapping component dimensions (or set elements/tokens) to identifiers (AdjType's element type is UInt32, plain token/set membership; other concrete adjacency element types, e.g. a compressed encoding, can be added by extending getcontainer, internal_push!, and sort_postinglist!). It always keeps the original indexed object in db.\n\nFields\n\ndist: distance function used at search time (e.g. Dist.Sets.Jaccard(), Dist.NormCosine()).\nadj: posting lists (non-zero id-elements, in rows).\nsizes: number of non-zero values in each element (non-zero values in columns).\ndb: the original indexed objects, one per identifier; always populated by push_item!/append_items!.\n\nFor a handful of distances (the set metrics in Dist.Sets, see InvertedFiles.has_exact_fastpath) the score computed while merging posting lists is already exact, at O(1) cost. For any other distance (including Dist.NormCosine), every merge candidate is instead scored by evaluating dist directly against the objects stored in db, so results for that path are exact too — the number of such evaluations (hence cost) is controlled by the t-threshold parameter of search; raise t above the default 1 to bound the number of real evaluations per query.\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.InvertedFile"},{"category":"type","location":"api/#SimilaritySearch.InvertedFiles.PostingList","page":"API","text":"struct PostingList\n\nA single posting list: the (sorted) identifiers of every object containing token tokenID, plain ids with no associated weight (InvertedFile never needs one – see identiterator).\n\n\n\n\n\n","title":"SimilaritySearch.InvertedFiles.PostingList"},{"category":"method","location":"api/#SimilaritySearch.append_items!-Union{Tuple{T}, Tuple{BM25InvertedFile, InvertedFileContext, AbstractVector{T}}} where T<:AbstractString","page":"API","text":"append_items!(idx::BM25InvertedFile, ctx::InvertedFileContext, corpus; kwargs...)\n\nAdds every document in corpus to idx, computing each one's bag of words under idx.voc first. corpus can hold raw text (AbstractString), already-tokenized TokenizedText, or pre-tokenized string vectors; a corpus of already-computed BOWs is accepted directly by the generic SimilaritySearch.append_items! method without going through this conversion. See also push_item!.\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\", \"hello there\"]; verbose=false);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> ctx = InvertedFileContext();\n\njulia> append_items!(invfile, ctx, [\"hello world\", \"hello there\"]);\n\njulia> length(invfile)\n2\n\n\n\n\n\n","title":"SimilaritySearch.append_items!"},{"category":"method","location":"api/#SimilaritySearch.push_item!-Union{Tuple{T}, Tuple{BM25InvertedFile, InvertedFileContext, T}} where T<:Union{AbstractString, TokenizedText, AbstractVector}","page":"API","text":"push_item!(idx::BM25InvertedFile, ctx::InvertedFileContext, doc)\n\nAdds a single document doc to idx, computing its bag of words under idx.voc first. doc can be raw text (AbstractString), already-tokenized TokenizedText, or a pre-tokenized string vector; an already-computed BOW is accepted directly by the generic SimilaritySearch.push_item! method without going through this conversion. See also append_items!.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> ctx = InvertedFileContext();\n\njulia> append_items!(invfile, ctx, corpus);\n\njulia> push_item!(invfile, ctx, \"hello again\");\n\njulia> length(invfile)\n3\n\n\n\n\n\n","title":"SimilaritySearch.push_item!"},{"category":"method","location":"api/#SimilaritySearch.search-Union{Tuple{T}, Tuple{BM25InvertedFile, InvertedFileContext, T, SimilaritySearch.PQueue.AbstractKnnQueue}} where T<:Union{AbstractString, TokenizedText}","page":"API","text":"search(idx::BM25InvertedFile, ctx::InvertedFileContext, qtext, res::AbstractKnnQueue; t::Int=1)\n\nSolves a top-k query over idx for qtext (raw text, TokenizedText, or an already-computed bag of words), accumulating matches into res (an AbstractKnnQueue, e.g. KnnSorted or KnnHeap). Documents are ranked by BM25 score (stored internally as a negative value in res, so lower \"distance\" still means better match, consistent with SimilaritySearch.jl's convention). Returns res.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> ctx = InvertedFileContext();\n\njulia> append_items!(invfile, ctx, corpus);\n\njulia> res = knnqueue(KnnSorted, 2);\n\njulia> search(invfile, ctx, \"hello\", res);\n\njulia> collect(IdView(res))\nUInt32[0x00000001, 0x00000002]\n\n\n\n\n\n","title":"SimilaritySearch.search"},{"category":"function","location":"api/#TextSearch.BM25._bm25_fused_index_and_grow!","page":"API","text":"_bm25_fused_index_and_grow!(idx, ctx, items, startID, n, tol=1e-6)\n\nEncodes each raw/BOW document in items[1:n] into its SparseVecView term-frequency representation, registers its postings into idx.adj, and stores the vector into idx.db – all in a single pass (this is the \"fused\" behavior append_items!/push_item! use for raw-text/ BOW input, kept for efficiency: unlike the generic InvertedFile, BM25InvertedFile's db element type is derived from its input, so there is no cheap way to grow db ahead of indexing for this path – see _index_block! for the decoupled path instead, used when db is grown directly with already-encoded SparseVecViews).\n\n\n\n\n\n","title":"TextSearch.BM25._bm25_fused_index_and_grow!"},{"category":"method","location":"api/#TextSearch.BM25._index_block!-Tuple{BM25InvertedFile, InvertedFileContext, Int64, Int64}","page":"API","text":"_index_block!(idx::BM25InvertedFile, ctx::InvertedFileContext, sp::Int, n::Int)\n\nDecoupled indexing path: builds postings and doclens for idx.db[sp:n], reading the already-encoded SparseVecViews directly out of db (no re-tokenization). Used by index! when db was grown directly (e.g. push_item!(database(idx), docvec)) rather than through the fused append_items!/push_item! entry points.\n\n\n\n\n\n","title":"TextSearch.BM25._index_block!"},{"category":"method","location":"api/#TextSearch.BM25.bm25_internal_push_object!-Tuple{BM25InvertedFile, Integer, Any, Float64}","page":"API","text":"bm25_internal_push_object!(idx, docID, obj, tol) -> (doclen, docvec)\n\nRegisters obj (a pair (tokenID, freq) iterable) into idx.adj under docID and builds its SparseVecView term-frequency representation (docvec, to be stored at docID in idx.db by the caller). Returns obj's total token count (doclen) and docvec.\n\n\n\n\n\n","title":"TextSearch.BM25.bm25_internal_push_object!"},{"category":"method","location":"api/#TextSearch.BM25.bm25_query_vector-Tuple{BM25InvertedFile, Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"bm25_query_vector(idx::BM25InvertedFile, q)\n\nConverts q (a bag of words – BOW/Dict, or anything else pairiterator accepts) into a SparseVector, for bm25score to use in onmatch!. Passes q through unchanged if it's already a SparseVectorLike.\n\n\n\n\n\n","title":"TextSearch.BM25.bm25_query_vector"},{"category":"method","location":"api/#TextSearch.BM25.bm25_register_postings!-Tuple{BM25InvertedFile, Integer, Any}","page":"API","text":"bm25_register_postings!(idx::BM25InvertedFile, docID::Integer, docvec) -> doclen\n\nRegisters an already-encoded document vector docvec (anything pairiterator-compatible, typically the SparseVecView already stored at idx.db[docID]) into idx.adj under docID, and returns its token count (doclen). This is the postings-only half of bm25_internal_push_object! – it does not parse/build a SparseVecView, since docvec is assumed to already be one (e.g. read back from idx.db by _index_block!).\n\n\n\n\n\n","title":"TextSearch.BM25.bm25_register_postings!"},{"category":"method","location":"api/#TextSearch.BM25.bm25doclen-Tuple{Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"bm25doclen(doc::SparseVectorLike)\n\nTotal token count of doc (a document's term-frequency sparse vector – a SparseVecView, as stored in BM25InvertedFile's db, or a SparseVector). Used by bm25score.\n\n\n\n\n\n","title":"TextSearch.BM25.bm25doclen"},{"category":"method","location":"api/#TextSearch.BM25.bm25score-Tuple{BM25Scorer, Vocabulary, Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}, Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"bm25score(bm25::BM25Scorer, voc::Vocabulary, query::SparseVectorLike, doc::SparseVectorLike)::Float32\n\nComputes the BM25 relevance score of doc for query – each a term-frequency sparse vector (a SparseVecView, e.g. one of BM25InvertedFile's own db entries via database, or a SparseVector) – by merging their nonzero indices (nzind, assumed sorted ascending) in a single linear pass and summing tokenscore at every token id present in both. Higher is more relevant. query's own frequencies are not used (BM25 doesn't weight by query-side term frequency), only which tokens it contains.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> bm25 = BM25Scorer(voc);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> ctx = InvertedFileContext();\n\njulia> append_items!(invfile, ctx, corpus);\n\njulia> bm25score(bm25, voc, database(invfile)[1], database(invfile)[1])  # \"hello world\" scored against itself\n2.9917173f0\n\njulia> bm25score(bm25, voc, database(invfile)[1], database(invfile)[2])  # \"hello world\" scored against \"hello there\"\n0.96917987f0\n\n\n\n\n\n","title":"TextSearch.BM25.bm25score"},{"category":"method","location":"api/#TextSearch.BM25.tokenscore-Tuple{BM25Scorer, Any, Any, Any}","page":"API","text":"tokenscore(bm25::BM25Scorer, toknumdocs, doclen, tokfreqindoc)\n\nComputes the BM25 contribution of a single token to a document's score, given the number of documents containing the token (toknumdocs, i.e. its document frequency), the document's length in tokens (doclen), and the token's frequency in the document (tokfreqindoc). Used internally by bm25score and BM25InvertedFile search.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> tokenscore(BM25Scorer(voc), 2, 3, 1)\n0.8908208f0\n\n\n\n\n\n","title":"TextSearch.BM25.tokenscore"},{"category":"type","location":"api/#TextSearch.BM25.BM25InvertedFile","page":"API","text":"BM25InvertedFile{AdjType<:AbstractAdjList,DbType<:AbstractDatabase} <: AbstractInvertedFile\n\nAn inverted-file index (built on top of SimilaritySearch.InvertedFiles) that answers approximate/exact top-k queries ranked by BM25 relevance. Build it with BM25InvertedFile(voc), populate it with append_items!/push_item!, and query it with search (from SimilaritySearch.jl).\n\nFollows the same design as SimilaritySearch.InvertedFiles.InvertedFile: adj only ever stores plain document ids (AdjType's element type is UInt32, exactly like the generic InvertedFile); every other per-document detail needed to score a match – term frequencies – is fetched from db instead of being duplicated into the posting lists.\n\nFields\n\nvoc: the Vocabulary shared by every indexed document (also used to tokenize/encode query text).\nbm25: the BM25Scorer used to rank matches.\nadj: the adjacency list of posting lists (one per token id), mapping each token to the ids of the documents containing it.\ndoclens: number of tokens per indexed document.\ndb: each indexed document's term-frequency vector, one SparseVecView (token id => UInt32 frequency) per document, always populated by push_item!/ append_items!; may hold more documents than have actually been indexed – see len.\nlen: number of documents already indexed (postings built); may be less than length(database(idx)) if db was grown directly (e.g. push_item!(database(idx), docvec)) without a following index! call to catch up. Growing db directly with pre-computed SparseVecViews and then calling index!(invfile, ctx) is supported and builds postings from the already-stored vectors; the raw-text/BOW-taking append_items!/push_item! methods remain fused (encode+store+register in one pass) for efficiency.\nsynonyms: nothing, or a synonym network (e.g. as produced by LSI.synonyms) used to expand queries – only when voc.textconfig.expand_query_synonyms is also true – via expand_synonyms!. Never applied to documents, only to queries.\n\nExample\n\njulia> using SimilaritySearch\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> ctx = InvertedFileContext();\n\njulia> append_items!(invfile, ctx, corpus);\n\njulia> length(invfile)\n3\n\njulia> res = knnqueue(KnnSorted, 2);\n\njulia> search(invfile, ctx, \"hello\", res);\n\njulia> collect(IdView(res))\nUInt32[0x00000001, 0x00000002]\n\n\n\n\n\n","title":"TextSearch.BM25.BM25InvertedFile"},{"category":"method","location":"api/#TextSearch.BM25.BM25InvertedFile-Tuple{Vocabulary}","page":"API","text":"BM25InvertedFile(voc::Vocabulary; k1=1.2f0, b=0.75f0, δ=1f0, synonyms=nothing)\n\nCreates an empty BM25InvertedFile, fitting its BM25Scorer from voc (see BM25Scorer(voc) for k1/b/δ). Populate it with append_items!/push_item!. Pass synonyms (e.g. as produced by LSI.synonyms) to enable query-time synonym expansion (also requires voc.textconfig.expand_query_synonyms = true; see expand_synonyms!).\n\nExample\n\njulia> voc = Vocabulary(TextConfig(), [\"hello world\"]; verbose=false);\n\njulia> invfile = BM25InvertedFile(voc);\n\njulia> length(invfile)\n0\n\n\n\n\n\n","title":"TextSearch.BM25.BM25InvertedFile"},{"category":"type","location":"api/#TextSearch.BM25.BM25Scorer","page":"API","text":"BM25Scorer\n\nPrecomputed coefficients for the Okapi BM25 (BM25+) scoring function, used to rank documents against a query given per-token document frequencies and document lengths. Build one with BM25Scorer(voc) or BM25Scorer(trainsize, avgdoclen); score individual (token-frequency, document-length) pairs with tokenscore, or whole query/document bags of words with bm25score. BM25InvertedFile uses a BM25Scorer internally to answer top-k queries efficiently.\n\nFields\n\nk1_plus_1, k1_mult_1_min_b, and k1_mult_b_div_avg_doc_len are combinations of the BM25 k1/b hyperparameters and the corpus' average document length, precomputed for faster scoring; δ is the BM25+ lower-bound correction term; trainsize is the number of documents the corpus statistics were computed from.\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> bm25 = BM25Scorer(voc);\n\njulia> bm25.trainsize\n3\n\n\n\n\n\n","title":"TextSearch.BM25.BM25Scorer"},{"category":"method","location":"api/#TextSearch.BM25.BM25Scorer-Tuple{Integer, AbstractFloat}","page":"API","text":"BM25Scorer(trainsize::Integer, avgdoclen::AbstractFloat; k1=1.2f0, b=0.75f0, δ=1f0)\n\nCreates a BM25Scorer for a corpus of trainsize documents with average document length avgdoclen. k1 controls term-frequency saturation, b controls document-length normalization, and δ is the BM25+ lower-bound correction.\n\nExample\n\njulia> bm25 = BM25Scorer(3, 3.5);\n\njulia> bm25.trainsize\n3\n\n\n\n\n\n","title":"TextSearch.BM25.BM25Scorer"},{"category":"method","location":"api/#TextSearch.BM25.BM25Scorer-Tuple{Vocabulary}","page":"API","text":"BM25Scorer(voc::Vocabulary; k1=1.2f0, b=0.75f0, δ=1f0)\n\nCreates a BM25Scorer using the training size and average document length already computed in voc (see trainsize and avgdoclen).\n\nExample\n\njulia> corpus = [\"hello world\", \"hello there\", \"the cat sat\"];\n\njulia> voc = Vocabulary(TextConfig(), corpus; verbose=false);\n\njulia> BM25Scorer(voc).trainsize\n3\n\n\n\n\n\n","title":"TextSearch.BM25.BM25Scorer"},{"category":"method","location":"api/#Base.empty!-Tuple{TextSearch.Tokenizer.TokenizerBuffer}","page":"API","text":"empty!(buff::TokenizerBuffer; normtext=true, tokens=true, unigrams=true)\n\nClears the requested scratch fields of buff in place. Returns buff.\n\n\n\n\n\n","title":"Base.empty!"},{"category":"method","location":"api/#TextSearch.Tokenizer.alltokengenerators-Tuple{TokenizationConfig}","page":"API","text":"alltokengenerators(cfg::TokenizationConfig)::Vector{AbstractTokenGenerator}\n\nBuilds the full, ordered list of AbstractTokenGenerators cfg runs: the built-in ones implied by cfg.nlist, followed by cfg.generators (any extra/custom generators). Called once per tokenize invocation.\n\nExample\n\njulia> alltokengenerators(TokenizationConfig(nlist=[1, 2]))\n2-element Vector{AbstractTokenGenerator}:\n UnigramGenerator()\n NWordGenerator(2)\n\n\n\n\n\n","title":"TextSearch.Tokenizer.alltokengenerators"},{"category":"method","location":"api/#TextSearch.Tokenizer.flush_token!-Tuple{TextSearch.Tokenizer.TokenizerBuffer, AbstractTokenTransformation, AbstractTokenGenerator, Bool}","page":"API","text":"flush_token!(buff::TokenizerBuffer, tt::AbstractTokenTransformation, gen::AbstractTokenGenerator, mark_token_type::Bool)\n\nPushes the token accumulated in buff.io to the token list, applying gen's tokentag (when mark_token_type) and transform hook; discards empty strings and tokens the transformation drops (returns nothing for).\n\n\n\n\n\n","title":"TextSearch.Tokenizer.flush_token!"},{"category":"method","location":"api/#TextSearch.Tokenizer.generate!-Tuple{UnigramGenerator, TextSearch.Tokenizer.TokenizerBuffer, AbstractTokenTransformation, Bool}","page":"API","text":"generate!(gen::AbstractTokenGenerator, buff::TokenizerBuffer, tt::AbstractTokenTransformation, mark_token_type::Bool)\n\nRuns gen over buff, appending its produced tokens to buff.tokens. Called by tokenize for every generator in alltokengenerators; a new AbstractTokenGenerator subtype implements this method to define what it does. UnigramGenerator's tokens are emitted as a side effect of the shared unigrams pass, so its own generate! is a no-op.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.generate!"},{"category":"function","location":"api/#TextSearch.Tokenizer.isemoji","page":"API","text":"isemoji(c::Char, emojis::Set{Char}=DEFAULT_EMOJIS)::Bool\n\nTests whether c is one of the emoji characters in emojis (by default, DEFAULT_EMOJIS, the set known to TextSearch, loaded from emojis.txt). Used by normalize_text when TextConfig's group_emo option is set.\n\nExample\n\njulia> isemoji('😀')\ntrue\n\njulia> isemoji('a')\nfalse\n\n\n\n\n\n","title":"TextSearch.Tokenizer.isemoji"},{"category":"method","location":"api/#TextSearch.Tokenizer.needs_unigrams-Tuple{AbstractTokenGenerator}","page":"API","text":"needs_unigrams(gen::AbstractTokenGenerator)::Bool\n\nWhether gen needs the shared word-level unigrams basis (see unigrams) computed before it runs. Defaults to false; NWordGenerator and UnigramGenerator override it to true.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.needs_unigrams"},{"category":"method","location":"api/#TextSearch.Tokenizer.normalize_text-Tuple{TextConfig, AbstractString, Vector{Char}}","page":"API","text":"normalize_text(config::TextConfig, text::AbstractString, output::Vector{Char}; limits::Bool=true, isnormalized::Bool=false)\n\nNormalizes a given text using the specified transformations of config. If isnormalized=true, skips preprocessing and normalization passes, writing text directly to output.\n\nExample\n\njulia> buff = Char[];\n\njulia> normalize_text(TextConfig(), \"Café\", buff);\n\njulia> String(buff)\n\" cafe \"\n\n\n\n\n\n","title":"TextSearch.Tokenizer.normalize_text"},{"category":"method","location":"api/#TextSearch.Tokenizer.normalize_text-Tuple{TextConfig, Any}","page":"API","text":"normalize_text(textconfig::TextConfig, text; limits::Bool=false, isnormalized::Bool=false)\n\nConvenience method that normalizes text under textconfig (see normalize_text(config, text, output; limits)) and returns the result as a String instead of writing into a caller-provided buffer.\n\nExample\n\njulia> normalize_text(TextConfig(), \"Café!!\")\n\"cafe!!\"\n\n\n\n\n\n","title":"TextSearch.Tokenizer.normalize_text"},{"category":"method","location":"api/#TextSearch.Tokenizer.nwords-Tuple{NWordGenerator, TextSearch.Tokenizer.TokenizerBuffer, AbstractTokenTransformation, Any}","page":"API","text":"nwords(gen::NWordGenerator, buff::TokenizerBuffer, tt::AbstractTokenTransformation, mark_token_type)\n\n\n\n\n\n","title":"TextSearch.Tokenizer.nwords"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokenize-Tuple{Function, TextConfig, AbstractString, TextSearch.Tokenizer.TokenizerBuffer}","page":"API","text":"tokenize(textconfig::TextConfig, text)\ntokenize(copy_::Function, textconfig::TextConfig, text)\n\ntokenize(textconfig::TextConfig, text, buff)\ntokenize(copy_::Function, textconfig::TextConfig, text, buff)\n\nTokenizes text using the given configuration. The tokenize makes heavy usage of buffers, and when these buffers are shared it is mandatory to create a copy of the result (buff.tokens).\n\nChange the default copy function to make an additional filtering of the tokens. You can also pass the identity function to avoid copying.\n\nExample\n\njulia> collect(tokenize(TextConfig(), \"Hello world!!\"))\n[\"hello\", \"world\", \"!!\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokenize"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokenize_corpus-Tuple{Function, TextConfig, Any}","page":"API","text":"tokenize_corpus(textconfig::TextConfig, arr; isnormalized::Bool=false, verbose=true)\ntokenize_corpus(copy_::Function, textconfig::TextConfig, arr; isnormalized::Bool=false, verbose=true)\n\nTokenize a list of texts. The copy_ function is passed to tokenize as first argument.\n\nExample\n\njulia> corpus = [\"hello world\", \"the cat sat\"];\n\njulia> toks = tokenize_corpus(TextConfig(), corpus; verbose=false);\n\njulia> collect(toks[1])\n[\"hello\", \"world\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokenize_corpus"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokenize_paragraphs-Tuple{AbstractString}","page":"API","text":"tokenize_paragraphs(text::AbstractString)::Vector{String}\ntokenize_paragraphs(textconfig::TextConfig, text::AbstractString)::Vector{String}\ntokenize_paragraphs([textconfig::TextConfig,] arr::AbstractVector)::Vector{String}\n\nSplits text into paragraphs separated by two or more newlines (\\n\\n+ or \\r\\n\\r\\n+). Trims leading and trailing whitespace from each paragraph and filters out empty paragraphs. When textconfig is provided, normalizes the text before paragraph splitting.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokenize_paragraphs"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokenize_sentences-Tuple{AbstractString}","page":"API","text":"tokenize_sentences(text::AbstractString)::Vector{String}\ntokenize_sentences(textconfig::TextConfig, text::AbstractString; isnormalized::Bool=false)::Vector{String}\ntokenize_sentences([textconfig::TextConfig,] arr::AbstractVector; isnormalized::Bool=false)::Vector{String}\n\nSplits text into sentences using sentence-ending punctuation (., !, ?) followed by whitespace or newlines. Trims leading and trailing whitespace from each sentence and filters out empty sentences. When textconfig is provided, normalizes each sentence after splitting.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokenize_sentences"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokenizerbuffer-Tuple{Any}","page":"API","text":"tokenizerbuffer(f)\n\nBorrows a TokenizerBuffer from Tokenizer's own pool, passes it to f, and returns it to the pool afterwards. Unlike the buffer-less tokenize methods (which release their borrowed buffer before returning), the buffer stays borrowed for the whole extent of f, so it is safe to alias its contents (e.g. via borrowtokenizedtext) as long as they are consumed inside f.\n\nExample\n\njulia> TextSearch.Tokenizer.tokenizerbuffer() do buff\n           tokenize(borrowtokenizedtext, TextConfig(), \"hello world\", buff) |> collect\n       end\n[\"hello\", \"world\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokenizerbuffer"},{"category":"method","location":"api/#TextSearch.Tokenizer.tokentag-Tuple{AbstractTokenGenerator}","page":"API","text":"tokentag(gen::AbstractTokenGenerator)::Union{Char,Nothing}\n\nThe single-character tag appended (as \\ttag) to every token gen produces when mark_token_type=true. Defaults to nothing (untagged).\n\n\n\n\n\n","title":"TextSearch.Tokenizer.tokentag"},{"category":"method","location":"api/#TextSearch.Tokenizer.transform-Tuple{AbstractTokenTransformation, AbstractTokenGenerator, Any}","page":"API","text":"transform(tt::AbstractTokenTransformation, gen::AbstractTokenGenerator, tok)\n\nHook applied in the tokenization stage to change the input token tok, produced by generator gen (e.g. a UnigramGenerator or NWordGenerator), if needed. For instance, it can be used to apply stemming or any other kind of normalization. Return nothing to ignore the tok occurrence (e.g., stop words).\n\nThe default falls through to identity for any gen a custom AbstractTokenTransformation doesn't specialize, so adding a new AbstractTokenGenerator kind never requires touching existing transformations. The built-in generators dispatch to the legacy transform_unigram/transform_nword names for backward compatibility with transformations written against those.\n\nExample\n\njulia> transform(IdentityTokenTransformation(), UnigramGenerator(), \"cat\")\n\"cat\"\n\n\n\n\n\n","title":"TextSearch.Tokenizer.transform"},{"category":"method","location":"api/#TextSearch.Tokenizer.transform_nword-Tuple{AbstractTokenTransformation, Any}","page":"API","text":"transform_nword(::AbstractTokenTransformation, tok)\n\nLegacy per-kind hook kept for backward compatibility; prefer specializing transform on AbstractTokenGenerator subtypes in new code. Called by the default transform method for NWordGenerator tokens. Return nothing to ignore the tok occurence (e.g., stop words).\n\n\n\n\n\n","title":"TextSearch.Tokenizer.transform_nword"},{"category":"method","location":"api/#TextSearch.Tokenizer.transform_unigram-Tuple{AbstractTokenTransformation, Any}","page":"API","text":"transform_unigram(::AbstractTokenTransformation, tok)\n\nLegacy per-kind hook kept for backward compatibility; prefer specializing transform on AbstractTokenGenerator subtypes in new code. Called by the default transform method for UnigramGenerator tokens. Return nothing to ignore the tok occurence (e.g., stop words).\n\n\n\n\n\n","title":"TextSearch.Tokenizer.transform_unigram"},{"category":"method","location":"api/#TextSearch.Tokenizer.unigrams-Tuple{TextSearch.Tokenizer.TokenizerBuffer, AbstractTokenTransformation}","page":"API","text":"unigrams(buff::TokenizerBuffer, tt::AbstractTokenTransformation)\n\nPerforms the word tokenization\n\n\n\n\n\n","title":"TextSearch.Tokenizer.unigrams"},{"category":"type","location":"api/#TextSearch.Tokenizer.AbstractTokenGenerator","page":"API","text":"AbstractTokenGenerator\n\nAbstract type for a single token-producing strategy inside a TokenizationConfig's generators list. TokenizationConfig's nlist keyword argument is convenience sugar that builds the built-in generators below; passing generators directly (or mixing in your own AbstractTokenGenerator subtype) is how new kinds of tokens can be added without touching TokenizationConfig or the tokenizer's dispatch logic (e.g. character q-grams, skip-grams, or collocations, none of which are built-in anymore).\n\nImplementing a new generator kind requires:\n\na struct <: AbstractTokenGenerator holding whatever parameters it needs;\nneeds_unigrams (defaults to false) if it needs the shared word-level unigrams basis computed first;\nTextSearch.Tokenizer.generate! performing the actual token production;\noptionally tokentag (defaults to nothing, i.e. untagged) for the single-character tag appended to each token when mark_token_type=true.\n\nThe transform hook already dispatches on AbstractTokenGenerator with an identity default, so a new generator kind is usable with any existing AbstractTokenTransformation without further changes.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.AbstractTokenGenerator"},{"category":"type","location":"api/#TextSearch.Tokenizer.AbstractTokenTransformation","page":"API","text":"AbstractTokenTransformation\n\nAbstract type for token transformation hooks applied during tokenization (see transform). A TextConfig holds one such transformation in its transformation field; it is applied to every generated token before it is pushed to the token list, and can be used to implement stemming, lemmatization, casing rules, or stopword removal (by returning nothing).\n\n\n\n\n\n","title":"TextSearch.Tokenizer.AbstractTokenTransformation"},{"category":"type","location":"api/#TextSearch.Tokenizer.ChainTransformation","page":"API","text":"ChainTransformation(list::AbstractVector{<:AbstractTokenTransformation})\n\nHolds an ordered sequence of AbstractTokenTransformations, applied one after the other over each token via transform; if any step returns nothing the token is dropped and the remaining steps are skipped.\n\nExample\n\njulia> ct = ChainTransformation([IdentityTokenTransformation(), IgnoreStopwords(Set([\"the\"]))]);\n\njulia> collect(tokenize(TextConfig(transformation=ct), \"the cat sat\"))\n[\"cat\", \"sat\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.ChainTransformation"},{"category":"type","location":"api/#TextSearch.Tokenizer.IdentityTokenTransformation","page":"API","text":"IdentityTokenTransformation()\n\nThe default, no-op AbstractTokenTransformation: every token is kept unchanged.\n\nExample\n\njulia> collect(tokenize(TextConfig(transformation=IdentityTokenTransformation()), \"the cat sat\"))\n[\"the\", \"cat\", \"sat\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.IdentityTokenTransformation"},{"category":"type","location":"api/#TextSearch.Tokenizer.IgnoreStopwords","page":"API","text":"IgnoreStopwords(stopwords::Set{String})\n\nAn AbstractTokenTransformation that discards unigrams found in stopwords (returns nothing for them, causing the tokenizer to drop the token) and passes every other token through unchanged.\n\nExample\n\njulia> cfg = TextConfig(transformation=IgnoreStopwords(Set([\"the\", \"a\"])));\n\njulia> collect(tokenize(cfg, \"the cat sat\"))\n[\"cat\", \"sat\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.IgnoreStopwords"},{"category":"type","location":"api/#TextSearch.Tokenizer.NWordGenerator","page":"API","text":"NWordGenerator(q)\n\nProduces word q-grams (q > 1) from the shared unigram basis (tagged 'n'). Built from TokenizationConfig's nlist keyword argument for every entry other than 1.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.NWordGenerator"},{"category":"type","location":"api/#TextSearch.Tokenizer.NormalizationConfig","page":"API","text":"NormalizationConfig(;\n    del_diac::Bool=true,\n    del_dup::Bool=false,\n    del_punc::Bool=false,\n    group_num::Bool=true,\n    group_url::Bool=true,\n    group_usr::Bool=false,\n    group_emo::Bool=false,\n    lc::Bool=true,\n    re_user::Regex=DEFAULT_RE_USER,\n    re_url::Regex=DEFAULT_RE_URL,\n    re_num::Regex=DEFAULT_RE_NUM,\n    emojis::Set{Char}=DEFAULT_EMOJIS\n)\n\nDefines the text normalization stage of a TextConfig (see its normalization field): utf8 normalization, character removal, whitespace normalization, casing, etc. Consumed by normalize_text.\n\ndel_diac: indicates if diacritic symbols should be removed\ndel_dup: indicates if duplicate contiguous symbols must be replaced for a single symbol\ndel_punc: indicates if punctuaction symbols must be removed\ngroup_num: indicates if numbers should be grouped _num\ngroup_url: indicates if urls should be grouped as _url\ngroup_usr: indicates if users (@usr) should be grouped as _usr\ngroup_emo: indicates if emojis should be grouped as _emo\nlc: indicates if the text should be normalized to lower case\nre_user, re_url, re_num: the regexes used to detect @user mentions, URLs, and numbers when their corresponding group_* flag is set (see normalize_text). Override them to customize detection (e.g. for a different language or domain).\nemojis: the set of emoji characters grouped when group_emo is set (see isemoji).\n\nExample\n\njulia> buff = Char[];\n\njulia> normalize_text(TextConfig(normalization=NormalizationConfig()), \"Café\", buff);\n\njulia> String(buff)\n\" cafe \"\n\n\n\n\n\n","title":"TextSearch.Tokenizer.NormalizationConfig"},{"category":"type","location":"api/#TextSearch.Tokenizer.SnowballTokenTransformation","page":"API","text":"SnowballTokenTransformation(stemmer)\n\nAn AbstractTokenTransformation that stems unigrams using stemmer (typically a Snowball.Stemmer). The struct itself has no dependency on the Snowball/Languages packages; loading them (using Snowball, Languages) activates the TextSearchSnowballExt package extension, which implements the actual stemming (transform_unigram) and adds the SnowballTokenTransformation(lang::Languages.Language) convenience constructor.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.SnowballTokenTransformation"},{"category":"type","location":"api/#TextSearch.Tokenizer.TextConfig","page":"API","text":"TextConfig(;\n    normalization::NormalizationConfig=NormalizationConfig(),\n    tokenization::TokenizationConfig=TokenizationConfig(),\n    transformation::AbstractTokenTransformation=IdentityTokenTransformation()\n)\n\nDefines a preprocessing and tokenization pipeline, composed of 3 independent stages:\n\nnormalization: a NormalizationConfig (utf8 normalization, character removal, whitespace normalization, casing, etc.).\ntokenization: a TokenizationConfig (unigrams, word n-grams, and any extra custom AbstractTokenGenerators).\ntransformation: an AbstractTokenTransformation applied to every generated token (e.g. stemming, lemmatization, or stopword removal).\nexpand_query_synonyms: whether query-time synonym expansion (see expand_synonyms!) should be applied when this config's queries are searched against an index carrying a synonym network (e.g. a TextInvertedFile built with synonyms=...). Has no effect on indexing: documents are never expanded, only queries.\n\nExample\n\njulia> cfg = TextConfig(tokenization=TokenizationConfig(nlist=[1]));\n\njulia> collect(tokenize(cfg, \"cats\"))\n[\"cats\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.TextConfig"},{"category":"type","location":"api/#TextSearch.Tokenizer.TokenizationConfig","page":"API","text":"TokenizationConfig(;\n    nlist::Vector=Int8[],\n    mark_token_type::Bool=true,\n    generators::Vector{<:AbstractTokenGenerator}=AbstractTokenGenerator[]\n)\n\nDefines the tokenization stage of a TextConfig (see its tokenization field): unigrams and word n-grams, computed from the output of the normalization stage.\n\nnlist: a list of words n-grams to use (1 emits plain unigrams via UnigramGenerator, any other value emits word n-grams via NWordGenerator).\nmark_token_type: each token is marked with its type (nword) when is true.\ngenerators: extra AbstractTokenGenerators to run in addition to the ones nlist builds; this is the extension point for adding new kinds of tokens (e.g. character q-grams, skip-grams, or collocations, none of which are built-in anymore) without needing a new TokenizationConfig keyword argument (see alltokengenerators).\n\nNote: If nlist and generators are both empty, then it defaults to nlist=[1]\n\nExample\n\njulia> cfg = TokenizationConfig(nlist=[1, 2]);\n\njulia> collect(tokenize(TextConfig(tokenization=cfg), \"cats sat\"))\n[\"cats\", \"sat\", \"cats sat\tn\"]\n\n\n\n\n\n","title":"TextSearch.Tokenizer.TokenizationConfig"},{"category":"type","location":"api/#TextSearch.Tokenizer.TokenizedText","page":"API","text":"TokenizedText(tokens::AbstractVector{String})\n\nWraps a list of string tokens for a single document. TokenizedText is TextSearch's universal contract for pre-tokenized text. It behaves like an AbstractVector{String} (supporting indexing, iteration, push!, append!, etc.) and is recognized by tokenize, Vocabulary, bagofwords, vectorize, and append_items! to bypass TextSearch's internal normalization and tokenization pipeline.\n\nUse TokenizedText when integrating external tokenizers (e.g., WordTokenizers.jl, HuggingFace/subword tokenizers, spaCy, or custom tokenization functions).\n\nExample\n\njulia> using TextSearch\n\n# Standard tokenization output:\njulia> collect(tokenize(TextConfig(), \"Hello world!!\"))\n3-element Vector{String}:\n \"hello\"\n \"world\"\n \"!!\"\n\n# Integrating an external tokenizer:\njulia> external_tokens = [\"custom\", \"tokenization\", \"output\"];\n\njulia> tok_doc = TokenizedText(external_tokens);\n\njulia> voc = Vocabulary(TextConfig(), [tok_doc]; verbose=false);\n\njulia> vocsize(voc)\n3\n\n\n\n\n\n","title":"TextSearch.Tokenizer.TokenizedText"},{"category":"type","location":"api/#TextSearch.Tokenizer.TokenizerBuffer","page":"API","text":"TokenizerBuffer(n=128)\n\nSelf-contained scratch space reused across tokenization calls to avoid reallocating on every call: normtext holds the normalized text, tokens accumulates the produced tokens, unigrams holds the word-level basis used by NWordGenerator (and any custom generator needing it), and io is scratch space for building individual token strings.\n\nTokenizer pools these internally for its own buffer-less convenience API (see tokenize); callers that need to hold a buffer across several calls (e.g. to safely alias its contents via borrowtokenizedtext) should borrow one from the same pool via tokenizerbuffer instead of constructing their own.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.TokenizerBuffer"},{"category":"type","location":"api/#TextSearch.Tokenizer.UnigramGenerator","page":"API","text":"UnigramGenerator()\n\nEmits the word-level unigrams themselves as output tokens (untagged). Built from TokenizationConfig's nlist keyword argument when it contains 1. Every other generator that needs the word-level basis (see needs_unigrams) triggers the same underlying computation regardless of whether UnigramGenerator is present — this generator only controls whether the plain words also appear in the output.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.UnigramGenerator"},{"category":"constant","location":"api/#TextSearch.Tokenizer.DEFAULT_EMOJIS","page":"API","text":"DEFAULT_EMOJIS::Set{Char}\n\nThe built-in emoji set (loaded from emojis.txt), used as TextConfig's default emojis field. Pass a different Set{Char} to TextConfig(; emojis=...) to override it.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.DEFAULT_EMOJIS"},{"category":"constant","location":"api/#TextSearch.Tokenizer.DEFAULT_RE_NUM","page":"API","text":"DEFAULT_RE_NUM::Regex\n\nThe built-in number regex, used as TextConfig's default re_num field. Pass a different Regex to TextConfig(; re_num=...) to override it.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.DEFAULT_RE_NUM"},{"category":"constant","location":"api/#TextSearch.Tokenizer.DEFAULT_RE_URL","page":"API","text":"DEFAULT_RE_URL::Regex\n\nThe built-in URL regex, used as TextConfig's default re_url field. Pass a different Regex to TextConfig(; re_url=...) to override it.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.DEFAULT_RE_URL"},{"category":"constant","location":"api/#TextSearch.Tokenizer.DEFAULT_RE_USER","page":"API","text":"DEFAULT_RE_USER::Regex\n\nThe built-in @user-mention regex, used as TextConfig's default re_user field. Pass a different Regex to TextConfig(; re_user=...) to override it.\n\n\n\n\n\n","title":"TextSearch.Tokenizer.DEFAULT_RE_USER"},{"category":"method","location":"api/#TextSearch.LSI._lanczos_svd-Tuple{AbstractMatrix, Integer}","page":"API","text":"_lanczos_svd(A, k) -> Union{Nothing,Tuple}\n\nTruncated SVD of A keeping the top k singular triplets via ARPACK's implicitly restarted Lanczos iteration (Arpack.svds): exact to working precision (measured ~3e-7 relative error on the singular values) while never forming a Gram matrix, which is what makes it both the accurate and the fast choice at scale.\n\nARPACK's own iteration is sequential and it is not re-entrant (unsynchronized static state, so it must not be called concurrently from multiple threads – LSI factorizes one batch at a time, so that is not a constraint here). It is not, however, serial in throughput: the heavy work goes to BLAS, so on a multicore host it does use many cores (~17 of 64 measured), just less effectively than a dense eigen, which is BLAS-3 rather than mostly BLAS-1/2.\n\nReturns nothing when ARPACK cannot deliver k converged triplets – either by failing to converge or by throwing – so the caller can fall back to the exact dense path rather than abort a long fit.\n\n\n\n\n\n","title":"TextSearch.LSI._lanczos_svd"},{"category":"function","location":"api/#TextSearch.LSI.synonyms","page":"API","text":"synonyms(lsi::LatentSemanticIndexing, k::Integer=8;\n         dist=Dist.Cosine(), normalize::Bool=true, verbose::Bool=true, approx=:auto,\n         construction_recall::Real=0.97, search_recall::Real=0.9) -> Dict{String,Vector{Pair{String,Float32}}}\n\nBuilds a synonym network from lsi's vocabulary embeddings (wordvectors); see the (voc, wordvecs, k) method above for the underlying algorithm and for what approx/ construction_recall/search_recall control. normalize is forwarded to wordvectors before searching.\n\nExample\n\nnet = synonyms(lsi, 5)\nnet[\"dog\"]   # [\"dogs\" => 0.02, \"puppy\" => 0.11, ...]\n\n\n\n\n\n","title":"TextSearch.LSI.synonyms"},{"category":"function","location":"api/#TextSearch.LSI.synonyms-2","page":"API","text":"synonyms(voc::Vocabulary, wordvecs::AbstractDatabase, k::Integer=8;\n         dist=Dist.Cosine(), verbose::Bool=true, approx=:auto,\n         construction_recall::Real=0.97, search_recall::Real=0.9)\n    -> Dict{String,Vector{Pair{String,Float32}}}\n\nBuilds a synonym network from voc's token embeddings in wordvecs (column t = embedding of token(voc, t), e.g. from wordvectors or an externally supplied matrix): for every vocabulary token, finds its k nearest neighbors (by dist, cosine by default) among all other tokens' embeddings, via SimilaritySearch.allknn. Returns a Dict mapping each token to a list of neighbor_token => distance pairs sorted by increasing distance (lower means more similar); the token itself is always excluded from its own neighbor list.\n\napprox selects how the all-pairs search is done, and matters enormously on real vocabularies – an exhaustive search is O(vocabulary²):\n\n:auto (default): approximate when length(wordvecs) > SYNONYMS_APPROX_THRESHOLD, exhaustive below it (where exhaustive is already fast and exact, so there is nothing to gain from approximating).\ntrue: always approximate – build a SearchGraph, autotuning construction to MinRecall(construction_recall) and then the search parameters to MinRecall(search_recall).\nfalse: always exhaustive, via ParallelExhaustiveSearch. Exact, and unusably slow past a few tens of thousands of tokens.\n\nExample\n\nnet = synonyms(voc, wordvectors(lsi), 5)\nnet[\"dog\"]   # [\"dogs\" => 0.02, \"puppy\" => 0.11, ...]\n\n\n\n\n\n","title":"TextSearch.LSI.synonyms"},{"category":"method","location":"api/#TextSearch.LSI.wordvectors-Tuple{LatentSemanticIndexing}","page":"API","text":"wordvectors(lsi::LatentSemanticIndexing; normalize::Bool=true) -> MatrixDatabase{Matrix{Float32}}\n\nReturns the LSI embedding of every vocabulary token, as a (outdim(lsi), vocsize(lsi)) matrix database – column t is the embedding of token(lsi.model, t). This is exactly lsi.P (optionally column-normalized): a document's LSI vector (via vectorize/ vectorize_corpus) is a weighted sum of its tokens' columns of lsi.P, so these per-token vectors live in the same projected space and are directly comparable to each other and to document vectors (e.g. via Dist.Cosine()/Dist.NormCosine()). Set normalize=false to keep the raw (scaling-adjusted) lsi.P columns instead of unit-normalizing them.\n\nExample\n\nX = wordvectors(lsi)   # (outdim(lsi), vocsize(lsi)) MatrixDatabase\nX[5]                   # the embedding of token(lsi.model, 5)\n\n\n\n\n\n","title":"TextSearch.LSI.wordvectors"},{"category":"method","location":"api/#TextSearch.vectorize!-Tuple{AbstractVector{Float32}, LatentSemanticIndexing, Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"vectorize!(out::AbstractVector{Float32}, lsi::LatentSemanticIndexing, vec::SparseVectorLike; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false)\nvectorize!(out::AbstractVector{Float32}, lsi::LatentSemanticIndexing, text; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false)\n\nProjects a document (sparse vector or raw text) into the lower-dimensional dense LSI space in-place into out.\n\n\n\n\n\n","title":"TextSearch.vectorize!"},{"category":"method","location":"api/#TextSearch.vectorize-Tuple{LatentSemanticIndexing, Any}","page":"API","text":"vectorize(lsi::LatentSemanticIndexing, text_or_sparsevec; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false)\n\nProjects a raw text or sparse vector into the dense LSI space, returning a Vector{Float32} of length outdim(lsi).\n\n\n\n\n\n","title":"TextSearch.vectorize"},{"category":"method","location":"api/#TextSearch.vectorize_corpus-Tuple{LatentSemanticIndexing, Any}","page":"API","text":"vectorize_corpus(lsi::LatentSemanticIndexing, corpus;\n                 normalize::Bool=true,\n                 minweight::Real=1e-6,\n                 isnormalized::Bool=false,\n                 verbose::Bool=true) -> MatrixDatabase{Matrix{Float32}}\n\nVectorizes every document in corpus into the dense LSI space in parallel across threads via @BATCHES, returning a MatrixDatabase of size (outdim(lsi), length(corpus)) ready for dense similarity search.\n\n\n\n\n\n","title":"TextSearch.vectorize_corpus"},{"category":"type","location":"api/#TextSearch.LSI.LatentSemanticIndexing","page":"API","text":"LatentSemanticIndexing{M<:AbstractMatrix{Float32}, VM<:VectorModel} <: TextModel\n\nLatent Semantic Indexing (LSI) model that projects sparse vector representations produced by a VectorModel into a lower-dimensional dense semantic space via Truncated Singular Value Decomposition (SVD).\n\nFields\n\nmodel: The underlying VectorModel used to tokenize and weight text.\nP: Dense projection matrix of size (k, m) where k = outdim and m = indim = vocsize(model).\ns: Vector of singular values of length k.\nk: Output dimension (k <= maxoutdim).\nmaxoutdim: Requested maximum output dimension (default: 128).\nscaling: Scaling applied to singular vectors (:none, :inv_singular_values, :singular_values).\n\n\n\n\n\n","title":"TextSearch.LSI.LatentSemanticIndexing"},{"category":"method","location":"api/#TextSearch.LSI.LatentSemanticIndexing-Tuple{Any}","page":"API","text":"LatentSemanticIndexing(corpus;\n                       config::TextConfig=TextConfig(),\n                       gw::GlobalWeighting=IdfWeighting(),\n                       lw::LocalWeighting=TfWeighting(),\n                       maxoutdim::Integer=128,\n                       normalize::Bool=true,\n                       minweight::Real=1e-6,\n                       isnormalized::Bool=false,\n                       verbose::Bool=true,\n                       scaling::Symbol=:none)\n\nConvenience constructor that builds an LSI model directly from a text corpus using default or provided TextConfig.\n\n\n\n\n\n","title":"TextSearch.LSI.LatentSemanticIndexing"},{"category":"method","location":"api/#TextSearch.LSI.LatentSemanticIndexing-Tuple{TextConfig, Any}","page":"API","text":"LatentSemanticIndexing(config::TextConfig, corpus;\n                       gw::GlobalWeighting=IdfWeighting(),\n                       lw::LocalWeighting=TfWeighting(),\n                       maxoutdim::Integer=128,\n                       normalize::Bool=true,\n                       minweight::Real=1e-6,\n                       isnormalized::Bool=false,\n                       verbose::Bool=true,\n                       scaling::Symbol=:none)\n\nConvenience constructor that builds a Vocabulary and VectorModel from config and corpus, then fits and returns a LatentSemanticIndexing model.\n\n\n\n\n\n","title":"TextSearch.LSI.LatentSemanticIndexing"},{"category":"method","location":"api/#TextSearch.LSI.LatentSemanticIndexing-Tuple{VectorModel, Any}","page":"API","text":"LatentSemanticIndexing(model::VectorModel, corpus;\n                       maxoutdim::Integer=128,\n                       normalize::Bool=true,\n                       minweight::Real=1e-6,\n                       isnormalized::Bool=false,\n                       verbose::Bool=true,\n                       scaling::Symbol=:none,\n                       factorization::Symbol=:auto)\n\nComputes a Latent Semantic Indexing (LSI) projection matrix from corpus weighted by model. corpus can be a collection of raw texts or pre-vectorized sparse vectors (AbstractVector{<:SparseVectorLike} or AbstractDatabase).\n\nKeyword Arguments\n\nmaxoutdim: Target embedding dimension (default: 128).\nnormalize: Whether to L2-normalize vectors during intermediate vectorization (default: true).\nminweight: Threshold below which sparse vector weights are dropped (default: 1e-6).\nisnormalized: Set to true if input texts are already normalized (default: false).\nverbose: Whether to display progress bar during corpus vectorization (default: true).\nscaling: Scaling factor applied to projection coordinates:\n:none (default): standard orthogonal concept projection P = U_k^T.\n:inv_singular_values: classical LSI document coordinate scaling P = Σk^{-1} Uk^T.\n:singular_values: singular value weighted projection P = Σk Uk^T.\nfactorization: how the truncated SVD is computed, which decides whether a large corpus is tractable at all:\n:auto (default): :full while min(vocsize, length(corpus)) is at most LSI_FULL_FACTORIZATION_MAX, :lanczos above it.\n:lanczos: _lanczos_svd – ARPACK's restarted Lanczos iteration. Exact to working precision and the fastest option at scale; falls back to :full if ARPACK fails to converge.\n:full: exact, via a dense Gram matrix and a complete eigen. Costs O(min(m,n)^3) time and min(m,n)^2 memory regardless of maxoutdim (it computes every eigenpair and keeps maxoutdim of them), so it is only appropriate for small corpora.\n\nBoth options are exact; the choice is purely about cost, so there is no accuracy knob to tune here.\n\n\n\n\n\n","title":"TextSearch.LSI.LatentSemanticIndexing"},{"category":"constant","location":"api/#TextSearch.LSI.LSI_FULL_FACTORIZATION_MAX","page":"API","text":"LSI_FULL_FACTORIZATION_MAX\n\nLargest Gram-matrix side (min(vocsize, ndocs)) for which factorization=:auto still uses the exact dense :full path. Above it, :auto switches to :lanczos: measured on Spanish Wikipedia slices, :full wins below a couple of thousand documents (n=2000: 4.6s vs 14.8s) and loses badly above (n=8000: 48.8s vs 11.5s), since its cost grows with the cube of this side while ARPACK's is driven by the number of nonzeros.\n\n\n\n\n\n","title":"TextSearch.LSI.LSI_FULL_FACTORIZATION_MAX"},{"category":"constant","location":"api/#TextSearch.LSI.SYNONYMS_APPROX_THRESHOLD","page":"API","text":"The vocabulary size past which synonyms' approx=:auto prefers an approximate index.\n\n\n\n\n\n","title":"TextSearch.LSI.SYNONYMS_APPROX_THRESHOLD"},{"category":"method","location":"api/#SimilaritySearch.Projections.bitsketch-Tuple{RandomIndexing, Union{AbstractString, SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"bitsketch(ri::RandomIndexing, doc; minweight::Real=1e-6, isnormalized::Bool=false) -> Vector{UInt64}\nvectorize(::Union{Type{BitSketch}, BitSketch}, ri::RandomIndexing, doc; minweight::Real=1e-6, isnormalized::Bool=false) -> Vector{UInt64}\n\nComputes a SimHash-style binary bit sketch (packed into UInt64 words) from a document projected by RandomIndexing.\n\n\n\n\n\n","title":"SimilaritySearch.Projections.bitsketch"},{"category":"method","location":"api/#TextSearch.vectorize!-Tuple{AbstractVector{Float32}, RandomIndexing, Union{SimilaritySearch.Special.Sparse.SparseVecView, SparseArrays.SparseVector}}","page":"API","text":"vectorize!(out::AbstractVector{Float32}, ri::RandomIndexing, vec::SparseVectorLike; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false)\nvectorize!(out::AbstractVector{Float32}, ri::RandomIndexing, text; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false)\n\nProjects a document (sparse vector or raw text) into the lower-dimensional dense Random Indexing space in-place into out.\n\n\n\n\n\n","title":"TextSearch.vectorize!"},{"category":"method","location":"api/#TextSearch.vectorize-Tuple{Module, RandomIndexing, Any}","page":"API","text":"vectorize(m::Module, ri::RandomIndexing, text_or_sparsevec; kwargs...)\n\nProjects a document into the Random Indexing space and quantizes it using SQu8 or SQgu8.\n\n\n\n\n\n","title":"TextSearch.vectorize"},{"category":"method","location":"api/#TextSearch.vectorize-Tuple{RandomIndexing, Any}","page":"API","text":"vectorize(ri::RandomIndexing, text_or_sparsevec; normalize::Bool=true, minweight::Real=1e-6, isnormalized::Bool=false) -> Vector{Float32}\n\nProjects a raw text or sparse vector into the dense Random Indexing space, returning a Vector{Float32} of length outdim(ri).\n\n\n\n\n\n","title":"TextSearch.vectorize"},{"category":"method","location":"api/#TextSearch.vectorize_corpus-Tuple{Module, RandomIndexing, Any}","page":"API","text":"vectorize_corpus(m::Module, ri::RandomIndexing, corpus; kwargs...)\n\nProjects an entire corpus with Random Indexing and quantizes it with SQu8 or SQgu8.\n\n\n\n\n\n","title":"TextSearch.vectorize_corpus"},{"category":"method","location":"api/#TextSearch.vectorize_corpus-Tuple{RandomIndexing, Any}","page":"API","text":"vectorize_corpus(ri::RandomIndexing, corpus;\n                 normalize::Bool=true,\n                 minweight::Real=1e-6,\n                 isnormalized::Bool=false,\n                 verbose::Bool=true) -> MatrixDatabase{Matrix{Float32}}\n\nVectorizes every document in corpus into the dense Random Indexing space in parallel across threads via @BATCHES, returning a MatrixDatabase of size (outdim(ri), length(corpus)) ready for dense similarity search.\n\n\n\n\n\n","title":"TextSearch.vectorize_corpus"},{"category":"type","location":"api/#TextSearch.RI.BitSketch","page":"API","text":"BitSketch\n\nType tag used to request binary SimHash-style bit sketches when calling vectorize or vectorize_corpus.\n\n\n\n\n\n","title":"TextSearch.RI.BitSketch"},{"category":"type","location":"api/#TextSearch.RI.RandomIndexing","page":"API","text":"RandomIndexing(model::VectorModel, corpus=nothing;\n               maxoutdim::Integer=1024,\n               method::Symbol=:gaussian,\n               rng::AbstractRNG=Random.default_rng())\n\nConstructs a RandomIndexing model from a VectorModel.\n\nArguments\n\nmodel: The vocabulary and weighting model.\ncorpus: Optional corpus parameter (ignored during construction, provided for API symmetry with LSI).\n\nKeyword Arguments\n\nmaxoutdim: Target projection dimension (default: 1024).\nmethod: Random projection algorithm:\n:gaussian (default): Gaussian random projection matrix with unit-norm columns.\n:qr: Orthonormal random projection matrix via QR factorization.\n:sparse_random (or :ternary): Sparse ternary random projection (±1 with sparse support).\nrng: Random number generator (default: Random.default_rng()).\n\n\n\n\n\n","title":"TextSearch.RI.RandomIndexing"},{"category":"type","location":"api/#TextSearch.RI.RandomIndexing-2","page":"API","text":"RandomIndexing{M<:AbstractMatrix{Float32}, VM<:VectorModel} <: TextModel\n\nRandom Indexing (RI) model that projects sparse vector representations produced by a VectorModel into a lower-dimensional dense semantic space via random projections (SimilaritySearch.Projections), with default output dimension maxoutdim=1024.\n\nFields\n\nmodel: The underlying VectorModel used to tokenize and weight text.\nP: Projection matrix of size (k, m) where k = outdim and m = indim = vocsize(model).\nk: Output dimension (k = maxoutdim).\nmaxoutdim: Target embedding dimension (default: 1024).\nmethod: Random projection method used (:gaussian, :qr, :sparse_random).\n\n\n\n\n\n","title":"TextSearch.RI.RandomIndexing"},{"category":"method","location":"api/#TextSearch.RI.RandomIndexing-Tuple{Any}","page":"API","text":"RandomIndexing(corpus;\n               config::TextConfig=TextConfig(),\n               maxoutdim::Integer=1024,\n               method::Symbol=:gaussian,\n               gw::GlobalWeighting=IdfWeighting(),\n               lw::LocalWeighting=TfWeighting(),\n               verbose::Bool=true,\n               rng::AbstractRNG=Random.default_rng())\n\nConvenience constructor: creates a RandomIndexing model directly from raw corpus using default text configuration.\n\n\n\n\n\n","title":"TextSearch.RI.RandomIndexing"},{"category":"method","location":"api/#TextSearch.RI.RandomIndexing-Tuple{TextConfig, Any}","page":"API","text":"RandomIndexing(config::TextConfig, corpus;\n               maxoutdim::Integer=1024,\n               method::Symbol=:gaussian,\n               gw::GlobalWeighting=IdfWeighting(),\n               lw::LocalWeighting=TfWeighting(),\n               minfreq::Integer=1,\n               maxfreq::Integer=0,\n               verbose::Bool=true,\n               rng::AbstractRNG=Random.default_rng())\n\nConvenience constructor: builds a Vocabulary and VectorModel from corpus using config, and then creates a RandomIndexing model.\n\n\n\n\n\n","title":"TextSearch.RI.RandomIndexing"},{"category":"section","location":"#TextSearch","page":"Home","text":"TextSearch.jl is a package to create vector representations of text, mostly, independently of the language. It is intended to be used with SimilaritySearch.jl, but can be used independetly if needed. TextSearch.jl was renamed from TextModel.jl to reflect its capabilities and mission.\n\nFor generic text analysis you should use other packages like TextAnalysis.jl.\n\nIt supports a number of simple text preprocessing functions, and three different kinds of tokenizers, i.e., word n-grams, character q-grams, and skip-grams. It supports creating multisets of tokens, commonly named bag of words (BOW). TextSearch.jl can produce sparse vector representations based on term-weighting schemes like TF, IDF, and TFIDF. It also supports term-weighting schemes designed to cope text classification tasks, mostly based on distributional representations.","title":"TextSearch"},{"category":"section","location":"#Installing","page":"Home","text":"You may install the package as follows\n\n] add TextSearch\n\nalso, you can run the set of tests as follows\n\n] test TextSearch","title":"Installing"},{"category":"section","location":"#Getting-started","page":"Home","text":"See the Tutorial for a hands-on walkthrough: building vocabularies and vector models over a real text corpus, indexing with both raw (vector-space) and BM25 inverted files, saving/loading indexes with JLD2, and composing or replacing the tokenizer with WordTokenizers.jl. The TextSearch API page has the full reference.","title":"Getting started"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"This tutorial walks through building, querying, persisting, and customizing text search indexes with TextSearch.jl and SimilaritySearch.jl. Every code block on this page is a real, executed Julia session (not hand-typed transcripts), so the output you see is always in sync with the current code.\n\nA few sections use packages beyond TextSearch/SimilaritySearch:\n\n] add JLD2 WordTokenizers\n\nJLD2.jl — saving/loading indexes to disk.\nWordTokenizers.jl — an alternative, general-purpose English tokenizer, used both to feed TextSearch's own pipeline (sentence splitting) and to replace it entirely.\n\nnote: A naming collision to know about\nBoth TextSearch and WordTokenizers export a function named tokenize. If you using both, calling tokenize unqualified is ambiguous — Julia will tell you so. Qualify it (TextSearch.tokenize(...) / WordTokenizers.tokenize(...)) whenever both packages are loaded together, as in the examples below.","title":"Tutorial"},{"category":"section","location":"tutorial/#A-small-corpus-from-Project-Gutenberg","page":"Tutorial","text":"As a running example we use Edgar Allan Poe's short story The Cask of Amontillado (1846), split into its 54 paragraphs — public domain, small enough to read in one sitting, and long enough to make search results meaningful. The text comes from Project Gutenberg; each paragraph below is one \"document\".\n\nlength(CASK_OF_AMONTILLADO), CASK_OF_AMONTILLADO[1]","title":"A small corpus from Project Gutenberg"},{"category":"section","location":"tutorial/#Building-a-vocabulary-and-a-vector-model","page":"Tutorial","text":"Vocabulary parses the corpus once and accumulates per-token statistics (TextConfig() defaults to word unigrams). A VectorModel then turns that vocabulary into a weighting scheme — here, classic TF-IDF — and vectorize_corpus applies it to every paragraph, producing one sparse SparseVector per document.\n\nvoc = Vocabulary(TextConfig(), CASK_OF_AMONTILLADO; verbose=false)\nvocsize(voc), trainsize(voc)\n\nmodel = VectorModel(IdfWeighting(), TfWeighting(), voc)\nvecs = vectorize_corpus(model, CASK_OF_AMONTILLADO)\nvecs[1]","title":"Building a vocabulary and a vector model"},{"category":"section","location":"tutorial/#Pruning-the-vocabulary-by-minimum-frequency","page":"Tutorial","text":"798 tokens out of a ~4000-word story is a lot — most of them are one-off words (hapax legomena) that add noise more than signal. Each entry of a Vocabulary carries its own occs (total occurrence count across the corpus) and ndocs (number of documents it appears in), so filter_tokens can prune by either:\n\nhapax_count = count(t -> t.occs == 1, voc[i] for i in eachindex(voc))\nvocsize(voc), hapax_count\n\npruned_voc = filter_tokens(t -> t.occs >= 3, voc)\nvocsize(pruned_voc)\n\nfilter_tokens returns a brand new Vocabulary — voc itself is untouched, so the rest of this tutorial keeps using the original, unpruned vocabulary. To actually build a model on the pruned vocabulary instead, just use pruned_voc in place of voc from here on (e.g. VectorModel(IdfWeighting(), TfWeighting(), pruned_voc)); words below the frequency cutoff are treated as out-of-vocabulary from then on, the same as any other unseen word.\n\nFiltering by document frequency (t.ndocs) instead of raw occurrence count is often a better cutoff for longer/multi-document corpora — it discards words that are common within a single document but never recur elsewhere, which raw frequency alone wouldn't catch:\n\npruned_by_docfreq = filter_tokens(t -> t.ndocs >= 3, voc)\nvocsize(pruned_by_docfreq)","title":"Pruning the vocabulary by minimum frequency"},{"category":"section","location":"tutorial/#Searching-with-a-raw-inverted-file-(vector-space-ranking)","page":"Tutorial","text":"WeightedInvertedFile indexes the weight vectors directly and ranks by a distance over them — cosine here, via NormCosine (SimilaritySearch's cosine distance, re-exported by TextSearch). This is the same kind of index you'd use for any sparse vector search, not just text.\n\nwif = WeightedInvertedFile(vocsize(voc))\nwctx = quietctx()\nappend_items!(wif, wctx, VectorDatabase(vecs))\n\nres = knnqueue(KnnSorted, 5)\nsearch(wif, wctx, vecs[1], res)\ncollect(IdView(res))\n\nThe first hit is paragraph 1 itself (distance 0 — a document is always its own nearest neighbor); the rest are the paragraphs whose TF-IDF vectors are closest to it. Querying with free text instead of an existing document's vector works the same way, through vectorize:\n\nqvec = vectorize(model, \"vector search library\")\nres = knnqueue(KnnSorted, 5)\nsearch(wif, wctx, qvec, res)\n[(id, first(CASK_OF_AMONTILLADO[id], 60)) for id in collect(IdView(res))]\n\nOnly one hit came back even though we asked for 5 — an inverted file can only rank documents that share at least one token with the query, and here just one paragraph happens to contain \"search\". Unsurprisingly, a 19th-century short story has nothing to do with vector search libraries anyway; every score here is essentially noise. Try a query drawn from the story itself, like \"amontillado nitre\" or \"trowel wall\", to see closer, more meaningful matches.","title":"Searching with a raw inverted file (vector-space ranking)"},{"category":"section","location":"tutorial/#Vector-arithmetic:-dot-products,-centroids,-and-normalization","page":"Tutorial","text":"Since paragraph and query vectors are SparseVectors (from SparseArrays.jl), ordinary LinearAlgebra/SparseArrays operations work on them directly — +, -, dot, norm, normalize!, and scalar *// all work out of the box, with no extra glue code from TextSearch. Two things make this useful: comparing documents/queries directly via dot, and building a query that represents more than one idea at once.\n\nusing LinearAlgebra, SparseArrays\n\nq_wine = vectorize(model, \"amontillado wine\")\nq_damp = vectorize(model, \"nitre damp catacombs\")\nnorm(q_wine), norm(q_damp)\n\nvectorize normalizes its output to unit length by default (normalize=true) — this is what makes dot directly meaningful as a similarity score: for unit (\"spherical\") vectors, the dot product is the cosine similarity, bounded the same way cosine similarity is. It also matches what WeightedInvertedFile itself assumes — its distance is a cheap running dot-product sum (see Dist.NormCosine's docstring), valid only when the vectors being compared are already unit length.\n\ndot(q_wine, q_damp)\n\nZero — these two queries share no vocabulary at all, so as far as the dot product is concerned they're unrelated (not literally \"opposite\", just orthogonal). Compare that to two paragraphs that are actually about the same scene:\n\ndot(vecs[51], vecs[52])  # two consecutive paragraphs of Fortunato's manic \"ha! ha!\" laughter\n\nTo search for both ideas at once — say, a query that's part \"the wine\", part \"the damp vaults\" — combine the two query vectors into their centroid and search with that, instead of running two separate queries and merging results by hand:\n\nq_both = centroid([q_wine, q_damp])\nnorm(q_both)\n\nres = knnqueue(KnnSorted, 5)\nsearch(wif, wctx, q_both, res)\n[(id, first(CASK_OF_AMONTILLADO[id], 60)) for id in collect(IdView(res))]\n\nThe results blend both themes, rather than only ever matching one or the other.","title":"Vector arithmetic: dot products, centroids, and normalization"},{"category":"section","location":"tutorial/#Why-normalize!-matters-here","page":"Tutorial","text":"centroid normalizes its output, but that alone doesn't make a fair blend — it fixes the final vector's length, not the direction that a plain sum already baked in. If the inputs going in aren't themselves unit vectors, whichever one happens to have the larger magnitude dominates the sum, and the final normalize! just rescales that already-skewed direction to length 1. A minimal example makes this concrete:\n\na = sparsevec([1], [1.0f0], 2)  # a unit vector, pointing along \"axis 1\"\nb = sparsevec([2], [1.0f0], 2)  # a unit vector, pointing along \"axis 2\"\ncentroid([a, b])                # evenly split between both directions, as expected\n\nb_scaled = b * 5.0f0   # same direction as `b`, but 5x the magnitude\nnorm(b_scaled)\n\ncentroid([a, b_scaled])  # direction is pulled almost entirely toward b, not an even blend\n\nvectorize's default normalize=true is precisely what saves you from this: every vector TextSearch itself produces already lies on the unit sphere, so summing any number of them and normalizing the result gives a genuinely even blend, as in q_both above. The failure mode above only bites when vectors come from somewhere vectorize didn't touch — built with normalize=false, assembled by hand, or imported from a different pipeline entirely. The fix is always the same: call normalize! on each vector individually before combining them, so every input to a centroid is on equal footing before it's summed.","title":"Why normalize! matters here"},{"category":"section","location":"tutorial/#Searching-with-BM25-(probabilistic-ranking)","page":"Tutorial","text":"BM25InvertedFile is a different index entirely: instead of building explicit weight vectors, it indexes the corpus's bags of words and a BM25Scorer directly, ranking by the Okapi BM25 formula. There's no separate VectorModel/ vectorize_corpus step — append_items! takes raw text (or TokenizedText, or a pre-computed BOW) and computes everything it needs from voc.\n\nbm25idx = BM25InvertedFile(voc)\nbctx = quietctx()\nappend_items!(bm25idx, bctx, CASK_OF_AMONTILLADO)\n\nres = knnqueue(KnnSorted, 5)\nsearch(bm25idx, bctx, \"amontillado nitre\", res)\n[(id, first(CASK_OF_AMONTILLADO[id], 60)) for id in collect(IdView(res))]\n\nBoth index types answer top-k queries the same way (append_items!/push_item! to build, search to query), so switching between them is mostly a matter of which one matches your ranking needs: WeightedInvertedFile for vector-space similarity over any weighting scheme you've built, BM25InvertedFile when you want BM25's document-length normalization and term-saturation behavior without hand-building vectors first.","title":"Searching with BM25 (probabilistic ranking)"},{"category":"section","location":"tutorial/#Saving-and-loading-indexes-with-JLD2","page":"Tutorial","text":"Every type used above — Vocabulary, VectorModel, BM25InvertedFile, WeightedInvertedFile — is a plain Julia struct, so JLD2.jl can save and load them directly with no special glue code.\n\nusing JLD2\n\npath = tempname() * \".jld2\"\njldsave(path; voc, model, bm25idx)\n\nloaded = load(path)\nvoc2, model2, bm25idx2 = loaded[\"voc\"], loaded[\"model\"], loaded[\"bm25idx\"]\nvectorize(model2, CASK_OF_AMONTILLADO[1]) == vecs[1]\n\nres = knnqueue(KnnSorted, 5)\nsearch(bm25idx2, quietctx(), \"amontillado nitre\", res)\ncollect(IdView(res))\n\nThe reloaded BM25InvertedFile answers the same query with the same ranking as the original — nothing needs to be rebuilt or refit.","title":"Saving and loading indexes with JLD2"},{"category":"section","location":"tutorial/#Working-with-Paragraphs,-Sentences,-and-External-Tokenizers","page":"Tutorial","text":"TextSearch's own tokenizer (TextConfig/tokenize) is fast, thread-safe, and tuned for domain-agnostic and informal text. For long documents or specialised NLP tasks, you may want to segment text into paragraphs or sentences, skip redundant text normalization, or replace TextSearch's tokenizer entirely with an external tokenizer.","title":"Working with Paragraphs, Sentences, and External Tokenizers"},{"category":"section","location":"tutorial/#Paragraph-and-Sentence-Tokenization","page":"Tutorial","text":"TextSearch provides built-in helper functions tokenize_paragraphs and tokenize_sentences to split long documents or text corpora into finer-grained chunks prior to vocabulary building or indexing:\n\n# Splitting long text into sentences using TextSearch's built-in sentence tokenizer:\nall_sentences = tokenize_sentences(CASK_OF_AMONTILLADO)\nlength(all_sentences), all_sentences[1]\n\nsentence_voc = Vocabulary(TextConfig(), all_sentences; verbose=false)\nvocsize(sentence_voc), trainsize(sentence_voc)\n\nYou can also use external sentence segmenters like WordTokenizers.split_sentences as a preprocessing step:\n\nusing WordTokenizers\n\nsentences = String[]\nfor paragraph in CASK_OF_AMONTILLADO\n    for s in split_sentences(paragraph)\n        push!(sentences, String(s))\n    end\nend\n\nlength(sentences), sentences[1]","title":"Paragraph and Sentence Tokenization"},{"category":"section","location":"tutorial/#Avoiding-Redundant-Normalization-with-isnormalized","page":"Tutorial","text":"When text has already been normalized (for example, during paragraph or sentence extraction), you can pass isnormalized=true to tokenize, Vocabulary, bagofwords, or vectorize to skip character-level case-folding, diacritics removal, and regex preprocessing:\n\ncfg = TextConfig(normalization=NormalizationConfig(lc=true, del_punc=true))\nnorm_sentences = tokenize_sentences(cfg, CASK_OF_AMONTILLADO)\n\n# Pass isnormalized=true to skip repeating the normalization step:\nnorm_voc = Vocabulary(cfg, norm_sentences; isnormalized=true, verbose=false)\nvocsize(norm_voc)","title":"Avoiding Redundant Normalization with isnormalized"},{"category":"section","location":"tutorial/#Integrating-External-Tokenizers-with-TokenizedText","page":"Tutorial","text":"If you want to use an external tokenizer (such as WordTokenizers.jl, HuggingFace / BPE / WordPiece subword tokenizers, spaCy, or custom regex tokenizers), wrap your token list in a TokenizedText.\n\nTokenizedText is TextSearch's universal contract for pre-tokenized documents: Vocabulary, bagofwords, vectorize, and append_items! all recognize TokenizedText and skip TextSearch's internal normalization and tokenization steps entirely, consuming your external tokens as-is.\n\n# Tokenize using WordTokenizers.jl (or any external tokenizer):\nwt_docs = [TokenizedText(String.(WordTokenizers.tokenize(lowercase(p)))) for p in CASK_OF_AMONTILLADO]\ncollect(wt_docs[1])[1:8]\n\n# Build Vocabulary directly from external TokenizedText documents:\nwt_voc = Vocabulary(TextConfig(), wt_docs; verbose=false)\nvocsize(wt_voc)\n\nTextConfig() is still passed when constructing Vocabulary — TextSearch retains it for later query processing — but because every document arrives as a TokenizedText, none of TextConfig's tokenization rules alter how your documents were split; the tokenization was performed entirely by your external tokenizer.","title":"Integrating External Tokenizers with TokenizedText"},{"category":"section","location":"tutorial/#Dense-Semantic-Representations-and-Dimensionality-Reduction","page":"Tutorial","text":"While inverted files (like InvertedFile and BM25InvertedFile) excel at sparse keyword-based retrieval, many natural language processing workflows benefit from dense semantic embeddings or compact binary sketches.\n\nTextSearch.jl provides two complementary techniques for dimensionality reduction and dense indexing:\n\nLatent Semantic Indexing (LSI): Low-rank matrix factorization via truncated SVD.\nRandom Indexing (RI): Fast projection via random matrices, with direct pipelines for 8-bit scalar quantization (SQu8, SQgu8) and binary bit sketches (BitSketch).\n\n","title":"Dense Semantic Representations & Dimensionality Reduction"},{"category":"section","location":"tutorial/#Latent-Semantic-Indexing-(LSI)","page":"Tutorial","text":"","title":"Latent Semantic Indexing (LSI)"},{"category":"section","location":"tutorial/#Where-is-LSI-useful?","page":"Tutorial","text":"Synonymy and polysemy resolution: By projecting the term-document matrix onto its principal singular vectors, LSI groups words that frequently co-occur into shared latent semantic dimensions. Documents using different words for the same concept (e.g., \"wine\" and \"amontillado\") are mapped close together in the dense space.\nNoise reduction and compact representations: Reduces large vocabularies (e.g., tens of thousands of terms) into a dense, low-dimensional space (typically 64 to 300 dimensions, default maxoutdim=128).\nDense index compatibility: Creates dense MatrixDatabase{Matrix{Float32}} collections that can be indexed with approximate nearest-neighbor graph structures like SearchGraph.","title":"Where is LSI useful?"},{"category":"section","location":"tutorial/#Building-and-Querying-an-LSI-Model","page":"Tutorial","text":"You can construct a LatentSemanticIndexing model directly from a VectorModel and a training corpus, or via convenience constructors:\n\n# Train an LSI model with 16 latent dimensions on our Gutenberg corpus\nlsi = LatentSemanticIndexing(CASK_OF_AMONTILLADO; maxoutdim=16, verbose=false)\nlsi\n\n# Project a single query document into a dense Float32 vector:\nq_vec = vectorize(lsi, \"wine vaults and connoisseur\")\n(length(q_vec), typeof(q_vec))\n\n# Vectorize the entire corpus into a dense MatrixDatabase in parallel:\nlsi_db = vectorize_corpus(lsi, CASK_OF_AMONTILLADO; verbose=false)\nsize(lsi_db.matrix)\n\nNow we can build a graph index (SearchGraph) or exact search (ExhaustiveSearch) from SimilaritySearch.jl using cosine distance:\n\n# Index and search the dense LSI space:\nsctx = SearchGraphContext()\nlsi_index = SearchGraph(Dist.NormCosine(), lsi_db)\nindex!(lsi_index, sctx)\n\nres = knnqueue(KnnSorted, 3)\nsearch(lsi_index, sctx, q_vec, res)\n[(id, first(CASK_OF_AMONTILLADO[id], 60) * \"...\") for id in collect(IdView(res))]","title":"Building and Querying an LSI Model"},{"category":"section","location":"tutorial/#Word-Embeddings-and-a-Synonym-Network","page":"Tutorial","text":"lsi.P is a (outdim(lsi), vocsize(lsi)) projection matrix – column t is already the LSI embedding of vocabulary token t (a document's vector is just a weighted sum of its tokens' columns), so the same trained model doubles as a source of word embeddings, with no extra training needed. wordvectors returns them as a MatrixDatabase, ready for the same kind of nearest-neighbor search used above for documents:\n\nW = wordvectors(lsi)\nsize(W.matrix)\n\nRunning allknn over that word-embedding space is exactly how the \"synonymy resolution\" mentioned earlier becomes concrete: words that tend to co-occur in similar contexts end up with nearby embeddings. synonyms wraps this into a token => [(neighbor, distance), ...] network in one call:\n\nnet = synonyms(lsi, 5; verbose=false)\nnet[\"wine\"]\n\nFor a small demo corpus like this one, don't expect polished synonym pairs – a handful of short paragraphs isn't enough text for the co-occurrence statistics LSI relies on to fully separate content words from frequent function words. On a real corpus (thousands of documents, a pruned vocabulary), the same call is a quick way to get a first synonym/related-terms network without training a dedicated word-embedding model.\n\n","title":"Word Embeddings and a Synonym Network"},{"category":"section","location":"tutorial/#Random-Indexing-(RI)-and-Quantization-Pipelines","page":"Tutorial","text":"","title":"Random Indexing (RI) & Quantization Pipelines"},{"category":"section","location":"tutorial/#Where-is-Random-Indexing-useful?","page":"Tutorial","text":"Streaming / incremental scenarios without global matrix factorization: Unlike LSI (which computes SVD across an entire static corpus), Random Indexing assigns fixed, pseudo-orthogonal random projection vectors to each vocabulary term. New documents can be projected immediately on the fly without re-training or re-factoring any matrix.\nFast computation with Johnson-Lindenstrauss guarantees: Preserves pairwise cosine distances with high probability while being computationally lightweight (:gaussian, :qr, or sparse ternary :sparse_random).\nExtreme memory compression and hardware acceleration:\nSQu8 / SQgu8 Scalar Quantization: Compresses embeddings from 32-bit Float32 down to 8-bit UInt8 (a 4× memory reduction) while retaining search accuracy.\nBitSketch (SimHash): Compresses projections into 64-bit packed bit words (UInt64), turning similarity search into lightning-fast Hamming distance evaluations using CPU POPCNT and XOR instructions (Dist.Bits.Hamming()).","title":"Where is Random Indexing useful?"},{"category":"section","location":"tutorial/#1.-Dense-Random-Indexing-(Float32)","page":"Tutorial","text":"# Build a Random Indexing model from the corpus (default maxoutdim=1024; using 64 here for illustration)\nri = RandomIndexing(CASK_OF_AMONTILLADO; maxoutdim=64, method=:gaussian, verbose=false)\nri\n\n# Vectorize corpus into dense Float32 MatrixDatabase\nri_db = vectorize_corpus(ri, CASK_OF_AMONTILLADO; verbose=false)\nsize(ri_db.matrix)","title":"1. Dense Random Indexing (Float32)"},{"category":"section","location":"tutorial/#2.-8-Bit-Scalar-Quantization-(SQu8-and-SQgu8)","page":"Tutorial","text":"For memory-constrained environments, vectorize_corpus can directly produce quantized databases:\n\nusing SimilaritySearch.ScalarQuant: SQu8, SQgu8\n\n# Per-column 8-bit quantization (SQu8):\nsqu8_db = vectorize_corpus(SQu8, ri, CASK_OF_AMONTILLADO; verbose=false)\n\n# Single query quantized to SQu8:\nq_squ8 = vectorize(SQu8, ri, \"damp vaults and catacombs\")\n\n# Search using SQu8.NormCosine():\nsqu8_index = ExhaustiveSearch(SQu8.NormCosine(), squ8_db)\nres_squ8 = knnqueue(KnnSorted, 2)\nsearch(squ8_index, GenericContext(), q_squ8, res_squ8)\n[(id, first(CASK_OF_AMONTILLADO[id], 60) * \"...\") for id in collect(IdView(res_squ8))]","title":"2. 8-Bit Scalar Quantization (SQu8 & SQgu8)"},{"category":"section","location":"tutorial/#3.-Binary-BitSketches-and-Hamming-Search-(BitSketch)","page":"Tutorial","text":"BitSketch uses SimHash-style sign-packing to generate compact binary fingerprints:\n\n# Create a 512-dimension RI model (packed into 8 UInt64 words (512 bits) per document)\nri_bits = RandomIndexing(CASK_OF_AMONTILLADO; maxoutdim=512, verbose=false)\n\n# Vectorize entire corpus into packed bit words (MatrixDatabase{Matrix{UInt64}}):\nbits_db = bitsketch(ri_bits, CASK_OF_AMONTILLADO; verbose=false)\n(typeof(bits_db), size(bits_db.matrix))\n\n# Query bit sketch:\nq_bits = bitsketch(ri_bits, \"damp vaults and catacombs\")\n\n# Ultra-fast Hamming distance search:\nbit_index = ExhaustiveSearch(Dist.Bits.Hamming(), bits_db)\nres_bits = knnqueue(KnnSorted, 2)\nsearch(bit_index, GenericContext(), q_bits, res_bits)\n[(id, first(CASK_OF_AMONTILLADO[id], 60) * \"...\") for id in collect(IdView(res_bits))]","title":"3. Binary BitSketches & Hamming Search (BitSketch)"},{"category":"section","location":"tutorial/#A-small-tweet-like-corpus","page":"Tutorial","text":"TextConfig has several options aimed specifically at short, informal, social-media text: grouping @mentions, URLs, and emoji into single normalized tokens instead of leaving them as noisy character soup. The messages below are a small illustrative set written to exercise these options (not scraped from a live feed, so the example needs no network access and no data-license considerations).\n\nusing TextSearch, SimilaritySearch\n\nquietctx() = InvertedFileContext(logger=SimilaritySearch.LogList(SimilaritySearch.AbstractLog[]))\n\ntweets = [\n    \"Just landed in Mexico City!! 🎉 cant wait to try the tacos @VisitMexico #travel\",\n    \"Ugh, stuck in traffic again on the highway :( #mondayblues\",\n    \"New paper on approximate similarity search is out! check it out https://example.org/paper\",\n    \"@juli_ai loved your talk on vector databases today, so insightful #ai #ml\",\n    \"Rainy day, perfect for reading a good book ☕📚\",\n    \"Why does @united keep cancelling flights?? this is the third time this month #travelfail\",\n    \"Excited to announce our new open source vector search release! https://github.com/example/repo #julialang\",\n    \"lol this meme is too real 😂😂😂 #mood\",\n    \"Can anyone recommend a good vector search library for Julia? asking for a friend @julialang\",\n    \"Beautiful sunset over the bay tonight 🌅 #nofilter\",\n]\n\ncfg = TextConfig(normalization=NormalizationConfig(group_usr=true, group_url=true, group_emo=true, del_punc=false))\ncollect(TextSearch.tokenize(cfg, tweets[1]))\n\n@VisitMexico collapsed to _usr, and the 🎉 emoji collapsed to 👾 — with group_emo=true, every emoji character is replaced by this single placeholder glyph before tokenization, so any emoji becomes the same token instead of each distinct emoji being its own rare, one-off token. #travel stayed intact — hashtags are treated as regular content, not stripped, since they usually carry meaning.\n\nvoc = Vocabulary(cfg, tweets; verbose=false)\nbm25idx = BM25InvertedFile(voc)\nctx = quietctx()\nappend_items!(bm25idx, ctx, tweets)\n\nres = knnqueue(KnnSorted, 3)\nsearch(bm25idx, ctx, \"vector search library\", res)\n[(id, tweets[id]) for id in collect(IdView(res))]\n\nThe top matches are exactly the three tweets that actually mention vector search — BM25 ranks them by how much of the query they cover and how rare/salient those terms are across the small corpus.","title":"A small tweet-like corpus"},{"category":"section","location":"tutorial/#Next-steps","page":"Tutorial","text":"See the TextSearch API page for the full reference — every function and type used above (and many more, including the lower-level building blocks in TextSearch.Intersections and TextSearch.InvertedFiles) is documented there with its own runnable example.","title":"Next steps"}]
}
